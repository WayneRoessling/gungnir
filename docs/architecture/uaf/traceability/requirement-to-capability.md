# requirement-to-capability Requirement to capability

**UAF definition.** The traceability between architecture requirements and the capabilities they are stated against.

**Purpose here.** Shows which requirements bear on each leaf capability and which capabilities each requirement names; a capability with no requirement is listed in the specification's coverage section, not inferred.

Status: generated by `tools/build_uaf.py` on 2026-09-04; do not edit by hand.


## Requirement to Capability

| Requirement | Capability |
|---|---|
| REQ-F-01 Observations from heterogeneous sensors are ingested through one governed gateway that validates and quarantines | CAP-1.1, CAP-1.2 |
| REQ-F-02 Every source is authenticated before its data enters the picture | CAP-1.2 |
| REQ-F-03 Sensor modes, tasking, and calibration are held in a registry and changeable by the sensor manager | CAP-1.3 |
| REQ-F-04 Coverage is computed and coverage gaps are detected and displayed | CAP-1.4 |
| REQ-F-05 Data from sources with different clocks is time-disciplined and skew is detected | CAP-1.5 |
| REQ-F-06 Cooperative identity from open protocols is decoded and fused as evidence | CAP-1.7 |
| REQ-F-07 A multi-sensor, multi-rate, out-of-sequence tracking pipeline maintains the picture | CAP-2.1, CAP-2.5 |
| REQ-F-08 Tracks carry a quality and a staleness that the interface renders differently from fresh data | CAP-2.2 |
| REQ-F-09 Classification and identification produce evidence with a margin rule and per-class thresholds set by policy | CAP-2.6 |
| REQ-F-10 Entities keep a global identity with lineage across sources and sessions | CAP-2.7 |
| REQ-F-11 Threats are scored against a defended-asset list with per-class lethality and asset weighting | CAP-3.1, CAP-3.2 |
| REQ-F-12 The system recommends a cheapest-adequate assignment of resources to tracks within readiness, geometry, and policy | CAP-3.3, CAP-3.4 |
| REQ-F-13 Every recommendation carries a rationale and its alternatives | CAP-3.5 |
| REQ-F-14 The queue orders pending decisions by priority and time remaining, and supports pre-delegation where policy allows | CAP-3.7 |
| REQ-F-15 A session is journaled, replayable, and reproduces the same picture | CAP-5.2 |
| REQ-F-16 Measures are computed from the journal without manual collation | CAP-5.3 |
| REQ-D-01 Shared types have exactly one owning crate; everything above re-exports | **none** |
| REQ-D-02 Every detection carries its source and its receipt time | CAP-1.2 |
| REQ-D-03 Identification evidence names its producer, including model name and version for a machine-learning source | **none** |
| REQ-D-04 Envelopes carry a bus sequence number and a mission time | **none** |
| REQ-D-05 The node journal fsyncs every envelope; desktop journals fsync on session save and every five seconds | **none** |
| REQ-D-06 Journals merge in mission-time order, duplicates are dropped, and conflicting decisions are reported rather than resolved | **none** |
| REQ-D-07 Views, reports, and the interface contract carry a releasability marking | CAP-6.6 |
| REQ-D-08 Training data and model artefacts live outside this repository; only the manifest enters it | **none** |
| REQ-I-01 The node exposes a versioned contract for snapshot, event stream, detection submission, and plan decisions | CAP-7.1 |
| REQ-I-02 Schema versions are negotiated explicitly and an incompatible peer is refused, not silently accepted | CAP-7.2 |
| REQ-I-03 Radar feeds decode from ASTERIX category 048 | **none** |
| REQ-I-04 Coalition track exchange uses STANAG 4676 | **none** |
| REQ-I-05 Each external party is a configurable endpoint with a message type rather than a bespoke integration | **none** |
| REQ-I-06 Detections are exportable in a columnar format for analytics and bulk exchange | **none** |
| REQ-S-01 No engagement, effector command, or reserved identity declaration executes without a recorded human decision | CAP-4.3 |
| REQ-S-02 Authority is enforced per role, per class, and per engagement layer, per the authority matrix | CAP-6.2 |
| REQ-S-03 Weapons control status is set by the authorized roles and constrains every recommendation | CAP-3.6 |
| REQ-S-04 Pending decisions escalate and time out rather than waiting indefinitely | CAP-3.6, CAP-3.7 |
| REQ-S-05 Every consequential act is attributable to a person, a role, and a mission time in an audit log | CAP-6.3 |
| REQ-S-06 Machine identities authenticate with mutual transport-layer security; operator sessions use short-lived signed tokens | **none** |
| REQ-S-07 Data is encrypted in transit on every network crossing and at rest where the deployment requires it | CAP-6.4 |
| REQ-S-08 The assistant has no tool that changes state and no dependency path to a crate that does | **none** |
| REQ-S-09 Untrusted text from sensors, peers, annotations, and reports is treated as data by the assistant, never as instruction | **none** |
| REQ-S-10 Every release carries a bill of materials, a signature, and its assurance reports | CAP-6.5 |
| REQ-P-01 The desktop sustains its frame-rate budget with the 3D viewport open under a dense-swarm scenario | **none** |
| REQ-P-02 The per-frame tick stays inside its budget with ingest, poll, plan, and journal all running | **none** |
| REQ-P-03 Snapshot calls do not touch the pipeline and stay inside their budget | **none** |
| REQ-P-04 Detection to on-screen update stays inside its end-to-end budget in the embedded profile | **none** |
| REQ-P-05 The node sustains its detection ingest throughput through validation and quarantine | **none** |
| REQ-P-06 Detection to event-stream publish stays inside its budget on-prem and in cloud | **none** |
| REQ-P-07 An accepted envelope is durable within its budget, and the node recovers to serving within its budget after restart | **none** |
| REQ-P-08 A desktop falls back to embedded operation within its budget after link loss, stores and forwards to capacity, and reconciles within its budget | **none** |
| REQ-P-09 Covariances stay positive semi-definite and no value becomes non-finite over long runs | **none** |
| REQ-U-01 Each role has a workspace containing what that role's threads need and not more | CAP-5.9 |
| REQ-U-02 Every layout carries a status strip stating what the system knows and does not know | **none** |
| REQ-U-03 Absence, staleness, and degradation are visible rather than inferred from silence | **none** |
| REQ-U-04 Machine assistance is labelled, sourced, and carries no authority in the interface | **none** |
| REQ-U-05 Default display vocabulary is the agreed standard terms, with a per-deployment override table | **none** |
| REQ-C-01 No dependency edge exists that `ARCHITECTURE.md` does not draw | **none** |
| REQ-C-02 Every dependency is recorded in the standards document §2.9 and pinned once | **none** |
| REQ-C-03 No `unwrap()` or `expect()` outside tests, entry points, and debug-only invariant checks | **none** |
| REQ-C-04 Unimplemented capability returns a named error on every reachable path | **none** |
| REQ-C-05 The graphics and compute contexts stay separate and separately pinned | **none** |
| REQ-C-06 Deployment profiles differ by configuration and wiring, never by conditional logic in a domain crate | **none** |
| REQ-C-07 The two binaries contain wiring, not domain logic | **none** |
| REQ-C-08 No pass criterion or measure target is widened to make a test pass | **none** |
| REQ-C-09 Every artefact is unclassified and derived from public sources | **none** |
| REQ-C-10 No verification gate is waived by an agent | **none** |

## Capability to Requirement

| Capability | Requirement |
|---|---|
| CAP-1.1 Ingest observations from every sensor class | REQ-F-01 |
| CAP-1.2 Validate, authenticate, and quarantine input | REQ-F-01, REQ-F-02, REQ-D-02 |
| CAP-1.3 Manage sensor readiness, modes, tasking, and calibration | REQ-F-03 |
| CAP-1.4 Know sensor coverage and gaps over terrain | REQ-F-04 |
| CAP-1.5 Keep time discipline | REQ-F-05 |
| CAP-1.6 Receive early warning, tracks, and reports from peers | **none** |
| CAP-1.7 Receive cooperative identity | REQ-F-06 |
| CAP-2.1 Maintain one multi-sensor track picture with uncertainty | REQ-F-07 |
| CAP-2.2 Keep tracks through gaps with visible quality | REQ-F-08 |
| CAP-2.3 Register sensors against each other | **none** |
| CAP-2.4 Track dense groups and count targets | **none** |
| CAP-2.5 Maintain a clutter-tolerant surface picture | REQ-F-07 |
| CAP-2.6 Classify and identify with evidence and confidence | REQ-F-09 |
| CAP-2.7 Keep one identity across gaps, sorties, sessions, and peers | REQ-F-10 |
| CAP-2.8 Predict trajectory, time to impact, closest approach | **none** |
| CAP-2.9 Detect anomalies in tracks and feeds | **none** |
| CAP-2.10 Fuse terrain, imagery, point clouds, and map layers | **none** |
| CAP-2.11 Answer geometric questions | **none** |
| CAP-2.12 Maintain pattern of life and order of battle | **none** |
| CAP-3.1 Maintain the defended-asset list | REQ-F-11 |
| CAP-3.2 Score threat and priority per track | REQ-F-11 |
| CAP-3.3 Recommend the cheapest adequate assignment | REQ-F-12 |
| CAP-3.4 Compute intercept geometry | REQ-F-12 |
| CAP-3.5 Offer alternatives, what-if, and rationale | REQ-F-13 |
| CAP-3.6 Enforce rules of engagement | REQ-S-03, REQ-S-04 |
| CAP-3.7 Manage the queue under saturation | REQ-F-14, REQ-S-04 |
| CAP-3.8 Recommend fires tasks with deconfliction | **none** |
| CAP-3.9 Recommend sensor re-tasking under degradation | **none** |
| CAP-4.1 Present recommendations for decision | **none** |
| CAP-4.2 Record every decision | **none** |
| CAP-4.3 Never execute without a recorded human decision | REQ-S-01 |
| CAP-4.4 Hand off to effector systems with provenance | **none** |
| CAP-4.5 Warn assets and authorities | **none** |
| CAP-4.6 Track engagements and assess effects | **none** |
| CAP-4.7 Assist roles without authority | **none** |
| CAP-5.1 Journal every event durably | **none** |
| CAP-5.2 Replay and rehearse | REQ-F-15 |
| CAP-5.3 Produce reports and measures from the journal | REQ-F-16 |
| CAP-5.4 Operate disconnected and reconcile | **none** |
| CAP-5.5 Report health honestly and run the alert lifecycle | **none** |
| CAP-5.6 Manage configuration baselines and mission plans | **none** |
| CAP-5.7 Govern algorithm and model baselines | **none** |
| CAP-5.8 Support the battle rhythm | **none** |
| CAP-5.9 Provide role-based workspaces and workflow | REQ-U-01 |
| CAP-5.10 Meet performance budgets per profile | **none** |
| CAP-6.1 Authenticate operators and callers | **none** |
| CAP-6.2 Authorize by role, class, and layer | REQ-S-02 |
| CAP-6.3 Audit everything that matters | REQ-S-05 |
| CAP-6.4 Protect data in transit and at rest | REQ-S-07 |
| CAP-6.5 Assure the supply chain | REQ-S-10 |
| CAP-6.6 Mark and enforce releasability | REQ-D-07 |
| CAP-6.7 Treat external input as untrusted | **none** |
| CAP-7.1 Expose a versioned interface | REQ-I-01 |
| CAP-7.2 Speak the domain's interoperability standards | REQ-I-02 |
| CAP-7.3 Run as desktop, on-prem node, or cloud node | **none** |
| CAP-7.4 Exchange with peers and coalition | **none** |

## Traceability

- Derives from: `../model/relationships.yaml` satisfies, generated from the requirements specification
- Feeds: plan 10 requirements management; the capability assessment
