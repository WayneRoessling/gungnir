# Decisions needed

Status: first draft, 2026-09-04. Gaps whose closure needs a scoping, policy, or
architecture decision from the owner before engineering can start or finish. Each
decision lists the gaps it unblocks; the register's entries name the decision in
their dependencies. Record the outcome in `data/decisions.yaml`, explain a technical
one in a `../../record/` item, and append a history entry to each gap it moves.

Generated by `tools/gen_gaps.py` from `data/`: edit the data and re-run the generator, never this file.

| Id | Decision | What is being decided | Where it is raised | Decider | Unblocks | Status |
|---|---|---|---|---|---|---|
| D-01 | Scope lock | Which crates and capabilities ship in the first release. Everything appropriate is scaffolded; the register assumes all of it is in scope until locked. | `../../../ARCHITECTURE.md` §10 | Owner | All gaps; the roadmap's I4 column | Resolved 2026-09-04 |
| D-02 | Credential mechanism | Mutual TLS or tokens for operators and API callers, and how sensor owners are issued credentials. | `../../../ARCHITECTURE.md` §10; `../../../ARCHITECTURE.md` §8.5 | Owner with the security engineer | GAP-002, GAP-041, GAP-057, GAP-060 | Resolved 2026-09-04 |
| D-03 | Lock the reconciliation and arbitration rules | Confirm `gungnir-resilience` merge semantics and `RoleRankArbiter` as the rules, or replace them, before failover is built on them. | `../../../ARCHITECTURE.md` §10 | Owner | GAP-050 | Resolved 2026-09-04 |
| D-04 | Confirm the performance budgets and the fsync policy | Every value in `../../performance-budgets.md` is a draft; the journal durability budget depends on the fsync policy. | `../../performance-budgets.md`; `../../../ARCHITECTURE.md` §10 | Owner with the deployment owner | GAP-056, GAP-067 | Resolved 2026-09-04 |
| D-05 | Adopt the proposed roles | Whether commander, planner, and intelligence analyst enter `gungnir-security::Role` now or stay proposed. | `../roles-and-stakeholders.md` §4; `../mission-analysis.md` §11 | Owner | GAP-033, GAP-055, GAP-058 | Resolved 2026-09-04 |
| D-06 | Releasability marking in this revision | Whether coalition data-exchange constraints are modelled now or deferred. | `../mission-analysis.md` §11 | Owner | GAP-062, GAP-065 | Resolved 2026-09-04 |
| D-07 | Fires in scope for the lead product | Whether the fires task (land, MT-06) is a first-release capability or a later domain extension. | `../land.md` §4.1; plans README primary-mission decision | Owner | GAP-036 | Resolved 2026-09-04 |
| D-08 | External agreements | Which warning channels and civil authorities, which peers and coalition partners, and which sensor and effector interfaces the product must integrate with; each needs an interface agreement before its adapter or endpoint can be specified. | `../air-defense-and-counter-uas.md` §3; `../intelligence.md` §6 | Owner | GAP-004, GAP-009, GAP-040, GAP-042, GAP-065 | Resolved 2026-09-04 |
| D-09 | Cooperative identity sources and codec corpora | Whether IFF (needs key material), ADS-B, and AIS are in scope, and how ASTERIX and STANAG 4676 test corpora and specification licences are obtained. | `../air-defense-and-counter-uas.md` §9; `../../verification-capability-table.md` §2 cross-layer row | Owner | GAP-010, GAP-064 | Resolved 2026-09-04 |
| D-10 | Repository hosting, runners, registry, vulnerability response | The CI and release workflows run only once the repository is hosted with runners; the container registry and the vulnerability-response objective are open in `../../release-governance.md`. | `../../release-governance.md`; `../../../ARCHITECTURE.md` §10 | Owner | GAP-061, GAP-067 | Resolved 2026-09-04, amended 2026-09-07 and 2026-09-08 |
| D-11 | `uuid` for `GlobalEntityId` | Whether to adopt `uuid` (a §2.9 sign-off) or keep the `u128` newtype. | `../../../ARCHITECTURE.md` §10 | Owner | None blocking | Resolved 2026-09-04 |
| D-12 | Default display vocabulary | Which nation's terminology the UI shows by default; the documents use function vocabulary with mappings in `../glossary.md`. | `../mission-analysis.md` §11 | Owner with plan 06 | GAP-055 | Resolved 2026-09-04 |
| D-13 | Home for anomaly-detection rules | An architecture decision: whether rule-based track and feed anomaly detection lives in `gungnir-analytics` (raising alerts through `gungnir-observability`) or in a new crate; either adds a dependency edge that `../../../ARCHITECTURE.md` must show first. | `../capabilities/capability-statements.md` CAP-2.9 | Owner with the services engineer | GAP-021 | Resolved 2026-09-04 |
| D-14 | Data-egress policy per profile for the assistant | What may leave a connected desktop or node to a cloud model, and what must stay local. | `../../plans/08-ai-agent-integration.md` | Owner | GAP-044 | Resolved 2026-09-04 |
| D-15 | Delegation policy | Which engagement cases may be pre-delegated to the operator or handled by policy under saturation, and what authority a disconnected desktop holds. | `../air-defense-and-counter-uas.md` §6; `../mission-threads.md` MT-10 | Owner | GAP-034, GAP-035, GAP-050 | Resolved 2026-09-04 |
| D-16 | Agree the open targets | The measures marked proposed or to be agreed in `../measures.md` and `../capabilities/measures-catalogue.md`, and the verification rows marked to be agreed, cannot become gates until the owner sets them. | `../capabilities/measures-catalogue.md`; `../../verification-capability-table.md` §2 | Owner | GAP-047, GAP-067 | Resolved 2026-09-04 |
| D-17 | Docking and multi-window | Whether operators need detachable panels or a second window (for example the map on one screen and the queue on another). The plan 06 designs assume one window with a fixed grid per role and collapsible panels; docking would add a dependency (`egui_dock` or similar, a §2.9 sign-off). | `../../ux/information-architecture.md` §1; `../../rust-ui-tech-stack-summary.md` §5 | Owner with the UI engineer | GAP-055 | Resolved 2026-09-04 |
| D-18 | API transport stack | Which HTTP, WebSocket, and TLS crates the `gungnir-api` transport is built on; a §2.9 sign-off, and the last decision standing between the decided contract and writing the transport. | `../../../ARCHITECTURE.md` §10; `../../gungnir-api-v1.md` | Owner with the services engineer | GAP-041, GAP-050, GAP-063, GAP-065 | Resolved 2026-09-05 |
| D-19 | Docking crate | Which docking crate implements the in-window half of D-17: `egui_tiles` or `egui_dock`. The last open §2.9 question, and the only stack decision the UI work still needed. | `../../ux/ux-to-code-map.md` §2; `agentic-coding-standards.md` §2.9 | Owner with the UI engineer | GAP-075 | Resolved 2026-09-05 |
| D-22 | Cipher and test-certificate crates | Which authenticated cipher stands behind DN-22's `seal`/`unseal`, and how a test makes a certificate so mutual TLS can be verified at all. D-20 covered authentication and **explicitly not these**: `hmac` authenticates and does not encrypt, and the workspace has no certificate-generation crate and must not gain a checked-in private key. DN-22 amendment 1 (§9) recommends `aes-gcm` for the cipher and `rcgen` as a dev-dependency only. | `../../design/DN-22-key-management.md` §9; `agentic-coding-standards.md` §2.9 | Owner with the security engineer | GAP-060, GAP-084 | Resolved 2026-09-05 |
| D-23 | Heartbeat interval and the connectivity budget | The connectivity budget said fallback under 2 s from the last heartbeat; the node beat every 10 s with an independent 35 s timeout, so a node that went silent with the socket open -- the case the budget exists for -- was unnoticed for 35 s. Found by GAP-056's connectivity test. Tightening the timeout alone cannot meet the figure, and meeting it by force would make every 2 s stall on an intermittent link a full reconnect. | `../../performance-budgets.md` connectivity; `gungnir-api/src/transport.rs` | Owner | GAP-056 | Resolved 2026-09-06 |
| D-21 | gRPC as a second transport | Whether `tonic` and `prost` enter the stack for the gRPC surface `../../gungnir-api-v1.md` plans for peer C2 systems that cannot speak the v2 HTTP contract; a §2.9 sign-off that D-18 explicitly left open. | `agentic-coding-standards.md` §2.9 rule 5; `../../gungnir-api-v1.md` | Owner with the services engineer | None blocking | Resolved 2026-09-05 |
| D-20 | Authentication crates | Which key-derivation, message-authentication, and constant-time-comparison crates operator authentication is built on; a §2.9 sign-off. **No cryptographic crate is in the workspace at all**, and D-02 chose the mechanism without choosing the libraries -- the same gap D-18 filled for the transport before GAP-041 could be written. DN-23 §9 recommends `argon2` with `password-hash`, `hmac` with `sha2`, and `subtle`, and argues the load-bearing choice: a symmetric MAC rather than public-key signing, because the node issues and verifies its own tokens, and a public-key scheme would put a private key in the process -- the same conflict that stopped TLS in GAP-041. | `../../design/DN-23-operator-authentication.md` §9; `agentic-coding-standards.md` §2.9 | Owner with the security engineer | GAP-057, GAP-059 | Resolved 2026-09-05 |
| D-24 | AIS and ADS-B editions and fixtures | Which ITU-R M.1371 edition and which ICAO Doc 9871 edition the cooperative-identity codecs are written against, and which public captures may be fixtures under what terms -- the same decision GAP-064 needed for ASTERIX, without which a codec is written from memory. | `../../design/external-standards.md` §3, §4 | Owner | GAP-010 | Resolved 2026-09-06 |
| D-25 | GeoTIFF decoder crate | Whether a TIFF decoder enters §2.9 for GeoTIFF DEMs, and whether the georeferencing is read by a dependency or by this project; the ASCII-grid route needs no crate either way. | `agentic-coding-standards.md` §2.9; `../../verification-capability-table.md` §2, `gungnir-data` row | Owner | GAP-023 | Resolved 2026-09-06 |
| D-26 | Interceptor speed in the effector model | Whether DN-04's effector model gains a closing speed so GAP-031's geometry has an input, and in what form: one optional number, or an envelope. | `../../design/DN-04-effector-model.md` §9 | Owner | GAP-031 | Resolved 2026-09-06 |
| D-27 | The escrow key holder | Who holds the escrow key DN-22 §10 describes: a deployment's question the note deliberately did not answer. | `../../design/DN-22-key-management.md` §10, §11 | Owner | GAP-084 | Resolved 2026-09-06 |
| D-29 | An X.509 crate at runtime | The node's TLS identity must be a certificate over a key the P-256 provider holds and never exports; rcgen 0.14 signs through its `SigningKey` trait without seeing the private half, but §2.9 admits rcgen as a dev-dependency only. | `agentic-coding-standards.md` §2.9; `../../design/DN-22-key-management.md` §9a | Owner | GAP-060 | Resolved 2026-09-06 |
| D-30 | The SecurityOfficer role | DN-22 §11 (signed) names a role that operates nothing and may only recover an escrowed key; `gungnir_security::Role` has no such variant, and adding one touches every exhaustive match on `Role`. | `../../design/DN-22-key-management.md` §11; `../../design/DN-20-after-action-review.md` role table | Owner | GAP-084 | Resolved 2026-09-06 |
| D-31 | A YAML crate | Regenerating a test-track set from Rust byte for byte needs the YAML composition of `gen_tracks.py`; `serde_yaml` is deprecated and §2.9 has no YAML crate. | `agentic-coding-standards.md` §2.9 | Owner | GAP-016 | Resolved 2026-09-06 |
| D-33 | Whether SD-16 (Cursor-on-Target) is in scope, and what it is pinned to | D-01 locked the release to the capabilities and crates then registered, and DN-25 adds a standard that was not among them. Three sub-questions: is SD-16 release content or a later increment; is the schema pinned now or with the codec; and is the protobuf framing pinned at a commit of a GPLv3 repository. Extended 2026-09-07 by a fourth: the type tree, which gives the `type` attribute its meaning and which the schema pin does not cover. Extended 2026-09-08 by three more, out of `../../design/tak-interoperability-research.md` §7: (e) whether the framing is pinned now that a stock client is known to send it on the mesh; (f) how the codec learns the field numbers; (g) which client pair records the corpus. | `../../design/DN-25-cursor-on-target.md`; `../../design/external-standards.md` §5 | Owner | GAP-090, GAP-091 | Resolved 2026-09-06, amended 2026-09-07 and 2026-09-08 |
| D-32 | Who verifies and pins AIS and ADS-B | `external-standards.md` §3 and §4 hold candidates with named unverified claims (the M.1371-5 to -6 layout delta, the gpsd logs' licence header, a permissively licensed 1090ES capture); D-24 said pin both, verify first, and left who does the verifying open. | `../../design/external-standards.md` §3, §4 | Owner | GAP-010 | Resolved 2026-09-06 |
| D-28 | How MOP-37 gets its targets | The usability measure has no target because no session with participants has completed, and the test plan's round 1 was written for wireframes the built panels have outrun: run round 1 on the built panels, set provisional targets from the heuristic walkthrough, or leave it open until increment 3. | `../../ux/usability-test-plan.md` §7 | Owner with the plan 06 lead | GAP-074 | Resolved 2026-09-06 |
| D-34 | The repository's licence | The workspace shipped `license = "UNLICENSED"` and no `LICENSE` file, so nothing said what a reader of the hosted repository may do with the code, and plan 01 §8's licence-plus-support model had no instrument behind it. Three sub-questions: which licence; whether crediting Roessling Digital Solutions LLC is a request or an enforceable term; and whether contributions can be relicensed commercially. | `../../plans/01-product-business-plan.md` §8; `../../gungnir-workspace-structure.md`; D-10 (visibility left open) | Owner | GAP-061 | Resolved 2026-09-07 |
| D-35 | Theme tokens: flat constants, or a swappable Palette | An outside review of `gungnir-ui/src/theme.rs` (item 89) asked whether the theme's DS-01 tokens should become a `Palette` value so a night variant could be swapped in, or stay `Color32` constants read directly at 239 call sites across three crates. | `../../../ARCHITECTURE.md` §10 item 89; `../../ux/design-system.md` DS-01 | Owner | GAP-095 | Resolved 2026-09-06 |
| D-36 | Selection halo colour: white, or a fourth map colour | The review proposed a cyan selection halo so a selected glyph reads faster; DS-04 already reserves white as the one colour nothing else on the map uses, and the assignment line sat only ten units from the friendly frame's blue. | `../../../ARCHITECTURE.md` §10 item 89; `../../ux/design-system.md` DS-04 | Owner | None blocking | Resolved 2026-09-06 |
| D-37 | Compared numerals: monospace, or the proportional face | Time remaining, scores, positions, speeds, ages and evidence weights are numbers an operator compares down a column; the proportional face has no tabular figures, so a column of them does not line up. | `../../../ARCHITECTURE.md` §10 item 89; `../../ux/design-system.md` DS-05 | Owner | None blocking | Resolved 2026-09-06 |
| D-38 | Viewport surface colour and coverage-ring alpha | The same review's colour pass covered the viewport surfaces (`VIEWPORT_BACKGROUND`, the grid) and `COVERAGE_MAX_ALPHA` alongside the chrome tokens D-35 to D-37 settle. | `../../../ARCHITECTURE.md` §10 item 89; `../../ux/design-system.md` DS-01 | Owner | None blocking | Resolved 2026-09-06 |
| D-39 | OS-keystore crate | Which crate reaches the operating system's keystore behind `KeyProvider`'s persistent custody, the condition DN-22 amendment 3 (§12) named for retiring the passphrase-sealed file: "until a §2.9 decision admits an OS-keystore crate." The same decision closes GAP-057's and GAP-060's remaining items, both of which reduce to it from authentication and transport identity respectively. | `../../design/DN-22-key-management.md` §12; `agentic-coding-standards.md` §2.9 | Owner with the security engineer | GAP-057, GAP-060, GAP-084 | Resolved 2026-09-08 |
| D-40 | The inference runtime GAP-077 deferred | GAP-077 built `gungnir-ml`'s traits and `ModelSet` but deliberately deferred signing off an inference runtime under §2.9 on 2026-09-05, on two grounds: no model had been trained yet to need one, and `docs/ml/architecture.md`'s own condition for a native ML dependency -- a security reviewer appointed for it -- had not been met. Neither ground has since changed on its own; the question is whether to proceed anyway. | `../../plans/09-ml-model-integration.md`; `docs/ml/architecture.md`; `agentic-coding-standards.md` §2.9 | Owner | GAP-077, GAP-078, GAP-080 | Resolved 2026-09-08 |
| D-41 | Real-world CRS support for DEM and point-cloud files | Neither the DEM loader (GAP-023) nor the point-cloud loader (GAP-098) converts a file's coordinates: each requires the file already be in the deployment's local-ENU frame and refuses it by name otherwise, because the approved stack has never admitted a projection library. Most real DEM and LIDAR data ships in a real-world CRS (geographic WGS84 or a projected system such as UTM), so this is a live usability limit on both capabilities, not a hypothetical one. | `agentic-coding-standards.md` §2.9; `gungnir-config` (`validate_terrain`, `validate_point_cloud`); GAP-023, GAP-098 | Owner | GAP-023, GAP-098 | Resolved 2026-09-08 |
| D-42 | Cloud KMS crate(s) for the ManagedService custody profile | GAP-084 built the disconnected desktop's OS-keystore custody (D-39) but the cloud deployment profile's equivalent, `ManagedService`, has never been designed: DN-22 §5 names it and nothing implements it, and no §2.9 decision has admitted a cloud KMS crate for it. | `../../design/DN-22-key-management.md` §5; `agentic-coding-standards.md` §2.9 | Owner | GAP-084 | Resolved 2026-09-08 |
| D-43 | What `solve_assignment` promises about a finite total | `gungnir_association::solve_assignment` checks that every *entry* of a cost matrix is finite and says nothing about the *total* it returns. On an all-finite matrix with entries near the representable limit the sum of the selected entries overflows and the function returns `Ok` with `total_cost = -inf` (GAP-103, where the behaviour is reproduced and characterised). Three answers are available and they are mutually exclusive. **(a) Refuse the input**: state a precondition bounding entry magnitudes so that an `n`-term sum cannot overflow, and reject a matrix that breaks it. Keeps the promise absolute and fails early, but refuses matrices whose optimum is perfectly representable, and any bound tight enough to be safe is a magic number of the kind this module's own doc comment already rejects for the rectangular-padding case. **(b) Check the output**: keep accepting every finite matrix and return a new `AssociationError` when the accumulated total is not finite. Never refuses a solvable problem, but makes every caller handle an error no real cost matrix can produce, and throws away a pairing that GAP-103's measurements show is correct even when the total is not representable. **(c) State that no finite total was ever promised**: document that the returned total is IEEE-754 arithmetic over the selected entries and overflows exactly when the true optimum is not a representable `f64`. This is what the code does today, and GAP-103's measurements are the case for it being defensible rather than merely convenient; it leaves any caller reading `total_cost` to check it. The choice fixes what `../../verification-capability-table.md` §1's Hungarian / Jonker-Volgenant row must say about the domain and the promise -- it says nothing about either today -- and only then what `gungnir-fuzz`'s own postcondition should assert. | `../../verification-capability-table.md` §1 (Hungarian / Jonker-Volgenant, and the limits list beneath the §1 gate table); `gungnir-association/src/assignment.rs`; `gungnir-fuzz/fuzz_targets/cost_matrix_construction.rs`; `docs/agentic-workflow.md` (low-trust tier: numerical stability guarantees, no silent NaN propagation) | Owner with the tracking engineer | GAP-103 | Resolved 2026-09-09 |
| D-44 | STANAG 4676 in this release, or out of it | GAP-064's own rule is a pinned edition and a public sample before a codec is written, and `../../design/external-standards.md` §2 records that neither is obtainable: STANAG 4676 is the cover agreement, AEDP-12 is the implementation document, and no download link for either could be confirmed -- the note's own open action is that somebody with NSO access retrieves AEDP-12 Ed A v1 and records its marking. Meanwhile the entry gates GAP-001's last dependency and the whole of GAP-063, whose closing action says the 4676 row is all that is left of the conformance suite. Three answers: retrieve it, descope it, or write the codec from secondary descriptions -- the last being exactly the outcome GAP-064 exists to prevent. | `../../design/external-standards.md` §2; GAP-064; GAP-063 | Owner | GAP-064, GAP-063, GAP-001 | Resolved 2026-09-15 |
| D-45 | How predictions over a rehearsal become one first-engagement range | DN-02 §7 names the aggregation and specifies nothing, and three entries stop at it: GAP-020 (whose predictor half is built), GAP-045's rehearsal and GAP-087's panel, which has the column and no rule to fill it. The choice is what a laydown's first-engagement range means when a run produced many predictions per approach -- the worst case, the typical one, or a stated percentile -- and it is load-bearing because US-15 compares two laydowns on exactly that column. | `../../design/DN-02-prediction-and-approach.md` §7; `../../ux/usability-test-plan.md` US-15 | Owner | GAP-020, GAP-045, GAP-087 | Resolved 2026-09-15 |
| D-46 | The dense-group pipeline's default filter | `DenseGroupFilter::Phd` is the default and its doc comment gave the CPHD's then-unsigned derivation as the reason. That reason expired when item 109 signed CPHD and item 128 signed the LMB and the dense-group wiring, so the default is now a live choice rather than a consequence: PHD is cheaper and assumes a Poisson cardinality, CPHD estimates cardinality properly under clutter and costs more per scan. Item 128 named the question and deliberately did not answer it, flipping a pipeline's default being a behaviour change nobody had asked for. | `../../../ARCHITECTURE.md` §10 item 128; `gungnir-fusion-async` (`DenseGroupFilter`); GAP-015 | Owner with the tracking engineer | GAP-015 | Resolved 2026-09-15 |
| D-47 | Whether a node's TLS identities persist, and whether the two roles share one | Item 105 closed the provider-issued gap between a node's serving identity and its outbound peer-link identity and left the ephemeral-or-persistent one open for both: `issue_for_client` builds a fresh `P256KeyProvider` every start, so a node presents a new certificate after every restart and no peer can pin it. Making either survive a restart needs a `gungnir-security` generalisation -- the service-name parameterisation item 104 already made for `wrapping_secret` -- and a decision about whether the serving and outbound roles then share one persisted identity or hold two. | `../../../ARCHITECTURE.md` §10 item 105; `gungnir-node/src/main.rs::host_tls`; `../../design/DN-22-key-management.md` §5 | Owner with the security engineer | GAP-060 | Resolved 2026-09-15 |
| D-48 | What the wrapped-secret file records about the key that wrapped it | GAP-084 item (3): `AzureKeyVaultKeyService`'s unwrap asks Key Vault for the key's latest version, and `WRAPPED_SECRET_FILE` records nothing about the version a secret was actually wrapped under. A deployment that rotates its master key therefore holds a blob the service will not open -- and that blob is what unlocks the keystore protecting the journal. Recording the version changes both the file's format and what `CloudKeyService::wrap` returns, which is a persisted-format decision rather than a fix, and one no test here can verify against a real vault. | `../../design/DN-22-key-management.md` §14e; `gungnir-security/src/managed_service.rs`; `../../../ARCHITECTURE.md` §10 item 129 | Owner with the security engineer | GAP-084 | Resolved 2026-09-15 |
| D-49 | Whether GAP-082's central differential-test harness is still wanted | GAP-061 item (5). The differential suite that actually runs is distributed across the nine crates it verifies and is what Gate 1 gates; `docs/agentic-workflow.md` §1 still names `gungnir-oracle` as the vehicle, and GAP-082's closing action still carries a central harness as an intention. One of the two has to give. | `docs/agentic-workflow.md` §1; GAP-061 item (5); GAP-082 | Owner | GAP-061 | Resolved 2026-09-15 |
| D-50 | Which of GAP-045's three undesigned controls are scoped now | GAP-045's closing action names three things that are each a decision rather than a build: laydown-aware sensor geometry (a rehearsal cannot move the sensors a laydown declares, because `gungnir-scenario` accepts no placement override and no gap scopes one), the gap-acceptance control (US-16 waits on it and it has no design), and the rehearsal-gated submit (DN-26 §6 rule 4 deliberately built no adoption workflow to gate). The first is why round 1 needed a third laydown invented purely to produce a measurable coverage difference. | GAP-045; GAP-087; `../../design/DN-26-laydown-options.md` §6; `../../ux/usability-test-plan.md` US-16 | Owner with the plan 06 lead | GAP-045, GAP-087, GAP-074 | Resolved 2026-09-15 |
| D-51 | The CRS tail: whether `proj` becomes a default feature, and where the vertical datum lives | GAP-102 left both named. `proj` is behind a default-off feature because `proj-sys` links libproj and needs cmake and SQLite3, so CI is the only place the conversion has ever run; making it default puts that toolchain in front of every contributor and every `cargo deny` run. Separately, a converted height is the file's own vertical datum in metres rather than a WGS-84 ellipsoidal height, because no geoid model is applied -- GAP-102's own text calls that its own gap without filing one. | GAP-102 items (3) and (5); `docs/agentic-coding-standards.md` §2.9 (D-41); `deny.toml` | Owner | GAP-102 | Resolved 2026-09-15 |
| D-52 | Whether a cooperatively-identified UAS gets a platform class | GAP-101's consumer (item 129) submits identity evidence for a Category 129 contact and maps no platform class, so `assessment.lethality_by_class` weighs such a track 1.0 while AIS maps an M.1371-6 ship type to `surface.*` and ADS-B a DO-260B category to `air.crewed`. Category 129 carries a manufacturer and a model string allocated by a registration authority rather than a capability category, and the class catalogue draws its air classes by threat type -- so a mapping here would be a class invented at the consumer, or a new catalogue entry with a mission argument behind it. | `../../../ARCHITECTURE.md` §10 item 129; `gungnir-app/src/uas.rs`; `docs/test-tracks/classes.yaml`; GAP-027 | Owner | GAP-027, GAP-101 | Resolved 2026-09-15 |
| D-53 | How a reconciliation conflict is resolved | D-03 locked `RoleRankArbiter` as the rule for conflicting decisions -- the higher role wins, the earlier decision wins a tie -- and GAP-050 built reconciliation to report every conflict and leave each to a person on PN-18, so the rule was locked and never applied. A journaled decision also carried no role to rank it by, and the desktop's authority role never followed the signed-in account. Build the rule into reconciliation, or record that a person resolves every conflict. | `../../verification-capability-table.md` §2, the cross-layer Disconnected reconciliation row; `gungnir-app/src/failover.rs`; the GAP-067 walk (`../../record/2026-09-16/gap-067-walk.md`) | Owner | GAP-067 | Resolved 2026-09-16 |
| D-54 | Whether a tasking concurrence may name nobody | DN-11 amendment 1 b let a concurrence carry a role with nobody signed in (`Concurrence::UnattributedRole`) because no build had an operator session, and kept the CAP-2.12 criterion asking for a concurrence carrying an operator. Desktop sign-in has since shipped (GAP-057), and tasking still succeeded with nobody signed in. Enforce the operator, or amend the criterion to accept a role, which amendment 1 b declined. | `../../verification-capability-table.md` §2, the `gungnir-workflow` collection requirements and tasking concurrence row; DN-11 amendment 1 b; the GAP-067 walk (`../../record/2026-09-16/gap-067-walk.md`) | Owner | GAP-067 | Resolved 2026-09-16 |
| D-55 | Where plan decisions live when desktops are linked to a node | A node proposes plans and decides none, so every linked desktop queues, decides, engages and hands off the node's plans itself: two desktops on one node each decide the same plan without knowing, escalation reaches nobody, and no decision reaches the node's record (GAP-129). Should the node hold the queue, should desktops forward their decisions to it, or should decisions stay on desktops? | GAP-129; `../../design/DN-31-node-approval-queue.md` | Owner | GAP-129, GAP-132, GAP-133, GAP-134 | Resolved 2026-09-17 |
| D-56 | How decision, plan and queue-item identifiers become unique | `DecisionId`, `PlanId` and `PendingApprovalId` restart at 1 in every process, so they collide across desktops, nodes and restarts, and an effector report matches its handoff by `DecisionId` alone. UUID v7, a machine-prefixed u64, or node-minted identifiers? | GAP-129; `../../design/DN-31-node-approval-queue.md` §5.1 | Owner | GAP-130 | Resolved 2026-09-17 |
| D-57 | Where the shared decision path lives, and its edges | The policy chain, submission and sweep, deciding with engagement opening and the one handoff builder, and handoff delivery bookkeeping live only in `gungnir-app`. D-55 puts them in a library both binaries use: a new crate, `gungnir-command`, or `gungnir-workflow`? | `../../design/DN-31-node-approval-queue.md` §3 and §4 | Owner | GAP-131 | Resolved 2026-09-17 |
| D-58 | Whether reconciliation compares engagements as well as plans | A desktop cut off from its node plans from its own picture, so its plans never share identifiers with the node's, and reconciliation, which pairs decisions by plan, calls two engagements of the same track on the two sides of an outage no conflict. | D-53; `../../design/DN-31-node-approval-queue.md` §6.8 | Owner | GAP-134 | Resolved 2026-09-17 |
| D-59 | Whether a pre-delegated item escalates | DN-10 §5 says a pre-delegated item still expires and escalates; GAP-035's closing note says escalation is what it skips; the code escalates it and uses the flag only for display. Which rule does the node's queue follow? | DN-10 §5; GAP-035; `../../design/DN-31-node-approval-queue.md` §6.2 | Owner | GAP-132 | Resolved 2026-09-17 |
| D-60 | How identifiers are written on the wire and in journals | `DecisionId`, `PlanId` and `PendingApprovalId` hold a UUID v7 as a `u128` (D-56), and DN-31 §5.1 kept their JSON form a number, so that a pre-change journal reads because a u64 number is a valid u128. A 128-bit JSON number fails `serde_json::to_value`, is rounded to a float when parsed into a `serde_json::Value`, and cannot be read inside a field-tagged enum; the handoff posted to an effector and the handoff body published for exchange both pass through a `Value`, and with a numeric v7 identifier both became `null`. A number read through `serde_json`'s `arbitrary_precision`, raw JSON bodies on the two handoff paths, or a string? | GAP-130; `../../design/DN-31-node-approval-queue.md` §5.1 | Owner | GAP-130 | Resolved 2026-09-17 |
| D-61 | How identifiers are shown | A UUID v7 is 36 characters, and its leading digits are a millisecond timestamp, so the identifiers one session mints look alike at the start. Until GAP-130 panels, alerts, audit entries, journals and reports named decisions and plans by a small counter. Show the full UUID everywhere, a short tag, or a label each desktop assigns? | GAP-130; `../../design/DN-31-node-approval-queue.md` §5.1 | Owner | GAP-130 | Resolved 2026-09-17 |
| D-62 | Whether gate 6 ever enforces its threshold on this runner | Gate 6 compares criterion results against a baseline saved from `main` on `gungnir-rtx-5060ti`, the owner's own workstation. Three measurements of unchanged code moved ratios by up to 1.04, and every benchmark crossed the 10 percent threshold at least once, so `BENCH_REGRESSION_ENFORCE` stayed `"0"`. Enforce it on this host after further work (a multi-run baseline, criterion's own statistical comparison, a wider threshold), or accept that it never enforces here? | GAP-093; `../../record/2026-09-17/gate-6-noise-floor-and-core-pinning.md` | Owner | GAP-093 | Resolved 2026-09-17 |
| D-63 | Whether the self-hosted runner is installed as a service | `gungnir-rtx-5060ti` is an interactive `run.cmd` listener, so it goes offline whenever its window closes or the workstation sleeps; that interrupted four of the 2026-09-16 and 2026-09-17 measurement runs. Install it as a Windows service under an account of its own, or leave it interactive? | GAP-093; GAP-061 | Owner | GAP-093 | Resolved 2026-09-17 |
| D-64 | How a rehearsal observes a laydown's sensors | A rehearsal replays a fixture's recorded detections, captured once from the sensor positions in the fixture's own `sensors.json`, so selecting a different laydown changes nothing a rehearsal can show about sensors (GAP-105). Commit one pre-generated fixture per laydown, or generate the detections a laydown's sensors would have produced at rehearsal time, inside the production binary? | GAP-105; `../../design/DN-32-re-observation-for-a-laydown.md` | Owner | GAP-105 | Resolved 2026-09-16 |
| D-65 | Whether the decision path is human-owned wherever it runs | `docs/agentic-workflow.md`'s recommend-versus-act boundary names `gungnir-policy`'s verdict logic and `gungnir-command`'s approval workflow. DN-31 moved the rest of that path -- the policy chain over a plan, the queue's feeding and sweep, deciding with engagement opening and the one handoff builder -- out of `gungnir-app`, which the list never named, into `gungnir-approval` (GAP-131), and then on to a node, which the list never contemplated (GAP-132). Does the list follow the code? | GAP-131; GAP-132 | Owner | GAP-133 | Resolved 2026-09-17 |
| D-66 | The order a reconnecting desktop settles, forwards and switches back | DN-31 §6.8 has a desktop forward the decisions it took while cut off and then reconcile over the node's history. GAP-134's build found that order puts contradictory decisions on the node's record with nothing beside them saying which stands, and makes the merge read the desktop's own forwarded decisions back as the node's. Keep §6.8's order, or settle before forwarding? | GAP-134 | Owner | GAP-134 | Resolved 2026-09-17 |
| D-67 | How a desktop is named, and what it forwards when its key is ephemeral | Every desktop's link identity carried the common name `gungnir-app`, so a node could not tell two apart, and a forwarded decision's `origin` could say a desktop took it but not which (GAP-141). Should a desktop be named by something the deployment states, or by its own key? And what should a desktop whose key provider is unreachable, and whose identity is therefore ephemeral, forward as its origin? | GAP-141; GAP-137 waits on it | Owner | GAP-137 | Resolved 2026-09-22 |
| D-68 | How the node's own exchange set and a desktop's published set share one register | `NodeApi::publish_exchange` replaced the held set for an item, which is the right contract while one desktop writes to it. Two writers now exist: the node issues handoffs of its own since GAP-132, and a desktop that fell back publishes its whole set on reconnect (GAP-133, GAP-134). Each would silently overwrite the other, and which set a partner saw would depend on tick order. One set per producer merged on read, the node as the sole writer merging what desktops post, or append per handoff? | GAP-132; GAP-133; GAP-134; GAP-137 | Owner | GAP-137 | Resolved 2026-09-23 |
| D-69 | What makes a producer's exchange set stale, and what a partner is told about it | The exchange register holds one set per producer (D-68) and has no lifecycle. It lives in the node's memory, so a node restart loses every set and no desktop republishes until it next issues a handoff; and nothing forgets a producer that has gone away, which an ephemeral desktop (D-67) creates afresh every run. A republish interval a desktop keeps to, a node that asks for one on reconnect, or a set that expires with the session that wrote it? | GAP-137; GAP-145 | Owner | GAP-145 | Resolved 2026-09-23 |
| D-70 | Whose clock a node's deadline is drawn against, and when a console is told they differ | A node's queue item carries `expires_at` in the node's mission time, and PN-06 drew the countdown against the desktop's own clock. Both are wall clocks, so they agree only as far as the two machines do and nothing measured the difference (GAP-140). Should the node's time travel with the picture and the countdown follow it, and at what difference should a console be told the two clocks disagree at all? | GAP-133; GAP-140 | Owner | GAP-140 | Resolved 2026-09-23 |
| D-71 | What a desktop that has never reached its node does | A desktop judges its node's silence against the heartbeat, measured from the last thing heard. A link that has never been heard from has no such measure, so a desktop that signed in to an unreachable node was neither linked nor fallen back: it sat on a remote backend with a queue it could not read and a decision path it could not use, for ever (GAP-142). Is a node that has never answered silent, and from when? | GAP-134; GAP-142 | Owner | GAP-142 | Resolved 2026-09-23 |
| D-75 | What a desktop's link does with a publish the node refuses, and what the console is told | The link kept any exchange batch the node did not take and offered it again every forward tick, whatever the answer. An Operator's console is refused `403` on every publish, because `PUBLISH_EXCHANGE` is not an Operator's, so it retried four times a second for as long as it ran and said nothing anywhere (GAP-146). How should the link tell a refusal that cannot succeed from one that can, when should it ask again, and what should the console say? | GAP-145; GAP-146 | Owner | GAP-146 | Resolved 2026-09-25 |
| D-76 | How the desktop's exchange outbox is bounded | `queue_exchange` bounded nothing: one batch per handoff issued, for as long as the node did not take them (GAP-146). The detection outbox drops its oldest past a capacity and counts it, and `gungnir_resilience::StoreAndForwardQueue` (GAP-121) does the same for envelopes. Should the exchange outbox take either rule, or another? | GAP-146; GAP-121 | Owner | GAP-146 | Resolved 2026-09-25 |
| D-79 | Which layout engine the If-Sr diagrams are rendered by | `build_uaf.py` pinned every If-Sr diagram to smetana, PlantUML's Java port of Graphviz dot, as a measured choice for squarer layouts. Since GAP-132 smetana throws in `mincross` on the plans-effectors-handoff diagram, so that one SVG was rendered by Graphviz and every other If-Sr SVG by smetana, and a reader compared two engines' layouts in one family (GAP-139). Pin the engine per diagram, pin a PlantUML version with the defect fixed, or render the family through Graphviz? | GAP-132; GAP-139 | Owner | GAP-139 | Resolved 2026-09-25 |
| D-80 | Whether the unreachable plan-keyed decision type is removed or documented | `gungnir_api::v3::ApprovalRequest`, a plan decision naming its own operator in the body, has been reachable from no route since GAP-132, and `ApiHandler::decide`, the only thing naming it, has no implementor (GAP-138). Remove both, or keep them and say in the trait what implements it and when? | GAP-132; GAP-138 | Owner | GAP-138 | Resolved 2026-09-25 |
| D-77 | How the journal holds an envelope carrying a NaN or an infinity | JSON has no spelling for a non-finite float and `serde_json` writes one as `null`, so such an envelope made a journal line that could not be read back: mid-session it failed the whole session, as the last line it was dropped as torn (GAP-126). Should `append` refuse the envelope with a named error, or should the journal carry the value? | GAP-126 | Owner | GAP-126 | Resolved 2026-09-25 |
| D-78 | When the journal is purged, what a purge may never take, and who chooses the age | `RetentionPolicy` existed with a 90-day default and no code applied it, so journals grew without bound (GAP-122). Building the purge needs a production answer to when it runs, what "age" is measured from, what it must never remove, how an interrupted purge is survived and seen, and whether a deployment that never declared a policy starts deleting on an upgrade. | GAP-122 | Owner | GAP-122 | Resolved 2026-09-25 |
| D-83 | How the risk score reads time to impact | The `gungnir-assessment` criterion is a score monotonic in time to impact, and the score read range and whether a track closed, never how soon it arrived (GAP-124). Which time to impact -- at the current closing speed, or to the closest point of approach -- how does it combine with range, priority and lethality, and what does the track's covariance change? | GAP-124 | Owner | GAP-124 | Resolved 2026-09-25 |
| D-84 | What a sensor's azimuth sector is and where it is stated | `CoverageVolume` had no bearing, so the coverage-accuracy criterion's bearing clause could not be checked and a sectored sensor could not be represented (GAP-118). Give it a sector, or take bearing out of the criterion? And if a sector: stated how, against which north, and where -- the sensor, the laydown, or both? | GAP-118 | Owner | GAP-118 | Resolved 2026-09-25 |
| D-85 | What "identical projections" means for a plan kept from an earlier solve | The backend-switching row asks for identical `AppState` projections from one scenario through the embedded and the remote backends. A plan keeps the geometry, value and time of the solve that first made its pairing (GAP-097), and which solve that was depends on which frame first saw the asynchronous pipeline's report -- so two runs of the same embedded desktop can hold one pairing under different geometry. What does the parity test compare for the plan, and what does it compare exactly? | GAP-120 | Owner | GAP-120 | Resolved 2026-09-25 |
| D-86 | How a node's picture stays live on a linked desktop between snapshots | A linked desktop reads the node's snapshot once per connection and follows the stream, and the stream carried plans but no track and no health: nothing in the workspace produced a `TrackingEvent`, and a linked desktop's health was the link's (GAP-160, GAP-161). Should the node publish the track lifecycle and the desktop read the node's health events, or should the link re-read the snapshot on an interval? | GAP-120; GAP-160; GAP-161 | Owner | GAP-160, GAP-161 | Resolved 2026-09-25 |
| D-72 | What truth a rehearsal re-observes, when scans fall between truth records | DN-32 §5.1 had both generators write truth at the finest scan period any sensor uses, and a rehearsal refuse to interpolate, on the premise that most scans fall between truth samples. The build found that the generators observe each scan against the state of the first truth tick at or after it. Rewrite all ten sample sets at the finest scan period, or re-observe on the recording's own tick by the rule that made it? | GAP-105; `../../design/DN-32-re-observation-for-a-laydown.md` §12 | Owner | GAP-105 | Resolved 2026-09-25 |
| D-73 | Whether a recording's sensor losses and jamming apply to a deployment's sensors | A recording's `environment.json` holds its sea state and, per sensor, its losses and electronic-attack windows, which name the recording's own sensors. DN-32 §5.3 designed the sidecar and was silent on how a window naming the recording's sensor 1 meets a deployment's sensors. Apply it by identifier, transfer it by sensor type, or count it and not apply it? | GAP-105; `../../design/DN-32-re-observation-for-a-laydown.md` §5.3 and §12 | Owner | GAP-105 | Resolved 2026-09-25 |
| D-74 | How a rehearsal's random draws are seeded | DN-32 §7 seeded a rehearsal's stream from the recording's seed and the laydown's identifier, so the same laydown rehearsed twice gives the same detections. Seeded by the laydown, two laydowns draw differently everywhere, and a sensor both place identically detects differently under each -- so a comparison names sensors nothing moved. Keep the laydown in the seed, or key the draws by what they concern? | GAP-105; `../../design/DN-32-re-observation-for-a-laydown.md` §7 and §12 | Owner | GAP-105 | Resolved 2026-09-25 |
| D-89 | When a linked desktop renews its session with the node | A node's token lapses after its lifetime and the stream never asks for another, so a linked desktop's writes are refused from then on (GAP-165). Should the link renew on a timer ahead of expiry, only when the node refuses a token, or should the node stop expiring tokens on a live stream? | GAP-120; GAP-165 | Owner | GAP-165 | Resolved 2026-09-26 |

## Outcomes

Recorded with the owner on 2026-09-04, and D-18 and D-19 on 2026-09-05. Technical
outcomes are explained in `../../record/`; mission outcomes in `../mission-analysis.md` §11.

| Id | Outcome |
|---|---|
| D-01 Scope lock | Everything in one release: all scaffolded crates and all 56 capabilities through I4 ship together. The register's I4 column is the release content; no crate is dropped at lock. |
| D-02 Credential mechanism | Mutual TLS for machine identities (desktops, nodes, peers, sensors); short-lived signed tokens for operator sessions issued by the node or a local identity provider; local accounts as the disconnected-desktop fallback. |
| D-03 Lock the reconciliation and arbitration rules | Defaults locked: journals merge in mission-time order, duplicates dropped, conflicting decisions reported; the higher role wins a conflict, the earlier decision wins a tie. |
| D-04 Confirm the performance budgets and the fsync policy | Draft budgets confirmed as provisional gates. Fsync per profile: the node journal fsyncs every envelope; desktop journals use buffered writes with fsync on session save and every 5 s. |
| D-05 Adopt the proposed roles | All three roles adopted: commander, planner, intelligence analyst. Eight roles total; the authority matrix is the specification (GAP-058); code change under GAP-068. |
| D-06 Releasability marking in this revision | Releasability modelled now: a marking field on views, reports, and the v1 contract in I3; enforcement per caller at the API in I4 (GAP-062). |
| D-07 Fires in scope for the lead product | Fires in the first release: GAP-036 moves to I3 alongside the allocator; fires deconfliction becomes part of the policy model. |
| D-08 External agreements | Generic endpoints now: each external party is a configurable endpoint with a message type in the v1 contract (warning, handoff, peer stream); real agreements are per deployment; synthetic peers for I3 and I4 work. |
| D-09 Cooperative identity sources and codec corpora | AIS and ADS-B decoders now from open protocols; IFF deferred (controlled key material); ASTERIX from the public Eurocontrol specifications and STANAG 4676 from the public NATO standard, synthetic and vendor-sample corpora only. |
| D-10 Repository hosting, runners, registry, vulnerability response | **Amended 2026-09-08: the `gpu` runner exists, and `gpu-fusion.yml` stays on `workflow_dispatch` permanently.** The runner the 2026-09-07 amendment said this workflow still needed is registered: `gungnir-rtx-5060ti`, labels `self-hosted, Windows, X64, gpu`, on the owner's workstation and its NVIDIA RTX 5060 Ti. **The trigger is not restored with it, and that is a decision rather than a wait.** A `workflow_dispatch` job on a self-hosted runner already *is* local execution -- the compute happens on the owner's hardware, and GitHub contributes the trigger and the log -- and dispatch can only be fired by a caller with write access, so no fork's pull request can reach the machine. A `pull_request` trigger would reverse exactly that: this repository is public, the runner is a workstation, and GitHub's own guidance is against the pairing. The cost accepted is that each GPU validation run is fired by hand (`gh workflow run gpu-fusion.yml`), which is a trivial cadence for a path that changes rarely. The alternative considered and declined was dropping the workflow and running `cargo test -p gungnir-data-fusion --features gpu-tests -- --ignored` on the host: it is the same command and the same hardware, but it produces no CI log, and `../../release-governance.md` defines the evidence package as the logs of every gate that ran, assembled by nobody. The repository's fork-PR approval policy is therefore left as it stands, because no self-hosted job is reachable from a pull request. **Amended 2026-09-07: GitHub, not a self-hosted GitLab.** The repository is hosted at `https://github.com/WayneRoessling/gungnir`, CI is GitHub Actions, and images go to the GitHub Container Registry. The eight workflows under `.github/workflows/` therefore stay where they are and are not ported, and the gate names and pass criteria do not change. **Two things the forge change does not remove.** `gpu-fusion.yml` still needs a self-hosted runner labelled `gpu` registered to the repository, because GitHub's hosted runners have no GPU -- that requirement belonged to the workflow rather than to the forge it ran on (`docs/agentic-workflow.md` already says so). And this workspace **is not a git repository**, so GAP-061 begins with an initial commit; the amendment changes where the history goes, not the fact that there is not one yet. **No forge credential appears in this repository or in any configuration baseline**, and none can: GitHub does not accept a password for git operations, so access is a personal access token or an SSH key held by the owner outside the workspace. Repository visibility is not fixed by this decision; nothing in the register depends on it, except that a public repository publishes the evidence package -- the `cargo deny` and `cargo audit` reports, the SBOM and the CI logs -- along with the code. The vulnerability-response objective is unchanged: critical advisories within 7 days of publication, high within 30 days, lower at the next release. **What this replaced**, resolved 2026-09-04: a self-hosted GitLab with its runners (including a GPU runner) and the GitLab Container Registry, with the GitHub workflows ported to GitLab CI under GAP-061. |
| D-11 `uuid` for `GlobalEntityId` | Adopt the `uuid` crate, UUID v7, for `GlobalEntityId`; §2.9 sign-off recorded; code change under GAP-069. |
| D-12 Default display vocabulary | NATO and joint terms from `../glossary.md` as the default UI labels, with a glossary override table in the configuration baseline per deployment (GAP-070). |
| D-13 Home for anomaly-detection rules | Detectors are pure functions in `gungnir-analytics` over track and health snapshots; the app and node tick call them and raise alerts through `gungnir-observability`. No new dependency edge. |
| D-14 Data-egress policy per profile for the assistant | Cloud profile: the cloud node may send the live picture to the cloud model. On-prem profile: derived text only, allow-listed per view kind. Disconnected profile: local model only. |
| D-15 Delegation policy | Supervisor may pre-delegate point-layer engagements of confirmed-hostile small UAS classes to operators per configuration; area layer and missiles never. A disconnected desktop keeps the delegations in force at disconnection, expiring after a configured interval; no new delegation while disconnected. |
| D-16 Agree the open targets | Walked measure by measure. Every proposed value confirmed or adjusted (raised MOE-08 to 0.9; tightened MOE-10 to 30 s and MOP-07 to 500 ms; loosened MOE-13 to 0.95, MOP-19 calibration optional, MOP-22 timing where supplied, MOP-23, MOP-27, and MOP-34 to two cycles; set MOP-04 under 1 per hour, MOP-32 under 500 ms, MOP-40 zero open findings); absolutes marked doctrine or table; MOP-25, MOP-35, MOP-37 deferred to plans 07, 08, 06. The six to-be-agreed verification rows were set: snapshot p99 1 ms, ICP 0.05 m and 0.5 mrad, GPU within 1e-3 and inlier ratio 0.01, egui pass p99 8 ms, coverage 1 percent and 0.1 degree, interop round-trip exact with best-effort decoding. Recorded in `../measures.md`, `../capabilities/measures-catalogue.md`, and `../../verification-capability-table.md` §2. |
| D-17 Docking and multi-window | Docking and multi-window where appropriate: panels may be docked and rearranged within the role's layout, and the viewport, the approval queue, and the replay timeline may be detached to a second window; decision dialogs stay with the queue; the arrangement is saved per role in the configuration baseline. Multi-window uses egui's native viewports; the docking crate was the remaining §2.9 question and was settled by D-19 (`egui_tiles`) on 2026-09-05. |
| D-18 API transport stack | Signed off: `axum` with its `ws` feature for the node's HTTP surface and the event stream, `tokio-tungstenite` for the desktop's WebSocket client, `reqwest` for its HTTP client, and `rustls`, `tokio-rustls`, and `rustls-pemfile` for the mutual TLS D-02 requires, with `tower-http` for request tracing and body limits. rustls rather than `native-tls`, so neither the node container nor the desktop needs a system OpenSSL and one TLS implementation appears in the SBOM; the pins keep exactly one `tungstenite` and one `tower-http` in the binary. Recorded in `agentic-coding-standards.md` §2.9 and in the workspace manifest; no member depends on them yet. gRPC is not included and stays a later question. |
| D-19 Docking crate | Signed off: `egui_tiles`, pinned to 0.10. That is the line depending on `egui ^0.29`, the workspace pin; `egui_tiles` 0.11 moved to `egui ^0.30` and would link a second `egui`. Resolution was checked on the pinned 1.98 toolchain against `eframe`/`egui` 0.29: `cargo tree -d` reports no duplicate `egui`, `eframe`, `emath`, or `epaint`. Chosen over `egui_dock` because a tree of tabs and splits maps directly onto the per-role panel lists `WorkspaceLayout::for_role` already produces, and fits the detached-window half of D-17 better. Detached panels themselves need no crate: egui 0.29's native viewports provide them. Recorded in `agentic-coding-standards.md` §2.9 and in the workspace manifest; no member depends on it yet, and it enters a member manifest under GAP-075. |
| D-22 Cipher and test-certificate crates | **Raised by GAP-060, which could not start without it.** DN-22 designs a custody boundary and names no algorithm; `seal` and `unseal` need an authenticated cipher, and none is in the workspace. Mutual TLS additionally needs certificates **in tests**, and a private key checked into the repository is key material in the repository whatever the comment above it says. Recommended: `aes-gcm` (AES-256-GCM, FIPS-approved and hardware-accelerated on both targets) and `rcgen` in `[dev-dependencies]` only. AES-256-GCM rather than ChaCha20-Poly1305 because both targets have AES hardware acceleration and AES-GCM is FIPS-approved; `rcgen` never ships, because a certificate generator in a deployed binary is a way to mint an identity. Duplicate linkage checked the same day. **The third row was taken later the same day**: `p256` (ECDSA P-256) for `KeyPurpose::BaselineSigning` and for the asymmetric provider a cloud deployment's TLS identity needs. It had been left open because no gap needed it, and what it was actually blocking was a *decision* rather than code -- GAP-060's remaining half could not be argued without it. P-256 rather than Ed25519 because it is FIPS-approved, which is what the accreditor these documents keep in view will ask; Ed25519 is the better curve on most other grounds and loses on that one. A signature scheme at all rather than reusing `hmac`, because DN-22 amendment 1 (a) needs a signature over a TLS transcript and there is no shared secret with the peer to MAC against. **In the workspace manifest and used by nothing**: no gap builds the asymmetric provider and none may depend on it until one does, the same condition D-21 put on `tonic`. Duplicate linkage checked, and checking it surfaced something worth writing down -- **an unused `[workspace.dependencies]` entry is never resolved at all**, so it is absent from the lockfile and `cargo tree -i` reports the package does not exist; the crate has to be added to a member temporarily and the lockfile read. Done that way: one copy each of `p256` 0.13.2, `elliptic-curve` 0.13.8, `ecdsa` 0.16.9, `primeorder` 0.13.6 and `sec1` 0.7.3, and no second copy of the `sha2`, `digest`, `generic-array`, `crypto-common`, `base64ct` or `subtle` beneath D-20's crates. |
| D-23 Heartbeat interval and the connectivity budget | **Recommended by the agent, accepted by the owner, implemented the same day.** The budget was conflating two things and both are now stated: **visibility** -- the operator can see the picture going stale within 2 s of the last beat, which needs no timeout because PN-01 shows 'heard N s ago' ticking up in real time -- and **declaration** -- the desktop decides the link is gone within four beats. `HEARTBEAT_INTERVAL` 10 s to 2 s: a WebSocket ping is a handful of bytes, half a ping per second per desktop and five per second per node at the ten-desktop fan-out budget, and at 10 s the display would have read '9 s' on a perfectly healthy quiet link. `HEARTBEAT_TIMEOUT` 35 s to 7 s, and **derived in code from the beat** (three tolerated misses plus a one-second margin) rather than written down beside it, because two independently edited constants is precisely how the conflict was created and how it would recur. Three misses rather than one so a single stall on an intermittent link does not cost a reconnect. Raising the budget to 35 s to match the code was rejected: that is the widen-the-criterion move the workspace forbids for verification rows, and the spirit applies. The plan was recorded here and executed in order: constants, the link's `last_heard`, `LinkFreshness` on PN-01 judged by the binary against the beat, the pinned test flipped from 'the budget is not met' to 'the relationship holds', and the documents. `gungnir-app` reaches the beat through `gungnir_remote::link`'s re-export rather than a new manifest edge to `gungnir-api`. |
| D-21 gRPC as a second transport | Signed off: `tonic` 0.14 and `prost` 0.14 for the gRPC surface, which D-18 had left a later question. Resolution checked the same day against the whole transport stack -- one `hyper` (1.11.1), one `tower` (0.5.3) and one `http` (1.5.0) shared with `axum` 0.8.9 -- because a gRPC stack that pulled a second HTTP implementation into the SBOM would have failed D-18's own test. In the workspace manifest and used by no member: **no gap builds the gRPC surface yet**, and one must be opened before a `.proto` schema or the `tonic-build` codegen dependency is added. It is a second transport and never a replacement; the v2 contract stays primary, and both are generated from the same `gungnir-model` types so that two transports do not become two contracts. |
| D-20 Authentication crates | **Signed off: `argon2` with `password-hash`, `hmac` with `sha2`, and `subtle`.** No cryptographic crate was in the workspace at all before this, and DN-23 needs a memory-hard key-derivation function for passphrases and a message authentication code for short-lived tokens. DN-23 §9 recommended `argon2` with `password-hash`, `hmac` with `sha2`, and `subtle`. **Signed off 2026-09-05.** The load-bearing choice is a symmetric MAC rather than public-key signing: D-02 has the node issue and verify its own tokens, so there is no third party to verify a signature, and a public-key scheme would put a private key in the process -- the conflict that stopped TLS in GAP-041 against DN-22's `seal`/`unseal` boundary. All RustCrypto, so one audit surface; resolution checked the same day, one copy each of the five and of `digest`, `generic-array`, `crypto-common` and `base64ct` beneath them. **Two purposes stay unnamed and are not covered**: DN-22 needs an authenticated cipher for the journal at rest and a signature scheme for baseline signing, and `hmac` authenticates without encrypting. Those are GAP-060 and GAP-084 to raise, with AES-256-GCM and ECDSA P-256 the recommendation to argue there. DN-23 itself remains an unsigned draft. |
| D-24 AIS and ADS-B editions and fixtures | **Pin both, verify first** (the recommended option). AIS: ITU-R M.1371-6 (02/2026) is current and M.1371-5 (02/2014) is what deployed transponders were certified to; the gpsd AIVDM/AIVDO document v1.58 is the public framing reference and gpsd's five AIS regression logs with their `.chk` decodes, BSD-2-Clause, are the fixture candidate. ADS-B: ICAO Doc 9871 2nd edition with Amendment 2 (2023), paid; *The 1090MHz Riddle* is CC BY-NC-SA 4.0 and flagged as reading only; pyModeS and dump1090 are GPL with no visible captures; a self-recorded 1090ES capture is recommended. All recorded in `external-standards.md` §3 and §4 with what was and was not verified. **Nothing is pinned**: the owner pins once the M.1371 -5 to -6 layout delta for the message types in scope is checked and the fixtures are copied with a `SOURCE.md`. No codec until then. |
| D-25 GeoTIFF decoder crate | **`tiff` (image-rs, MIT, pure Rust), and own the geo tags** (the recommended option). `tiff` 0.11 decodes the raster; `gungnir-data` reads `ModelPixelScale`, `ModelTiepoint`, `GeoKeyDirectory` and `GDAL_NODATA` itself per OGC GeoTIFF 1.1. ASCII grid first, GeoTIFF second, both landed the same day with repository-generated fixtures. `oxigdal-3d`, named by the 3D-data plan and never pinned, leaves the version table. |
| D-26 Interceptor speed in the effector model | **`intercept_speed_mps: Option<f64>`** (the recommended option): one closing speed for a constant-velocity closest-approach solution, not an envelope; `None` means no geometry for the resource, never a default. DN-04 amendment 1 (§9), signed by the owner the same day; the field, validation and test landed with it. Read by nothing until GAP-031's solver, which waits on GAP-029. |
| D-27 The escrow key holder | **A named security-officer role, per deployment** (the recommended option). DN-22 amendment 2 (§11) records it: a role that operates nothing and may only recover, held by a named account; journal data keys wrapped to the officer's public key (P-256 ECDH, HKDF, AES-GCM, all already in the stack), the private half never on a node, recovery offline and audited under `KEY_ESCROW_RECOVER`, the public key inline in the baseline and never a path. Signed by the owner the same day; unbuilt: waits on the asymmetric provider. |
| D-29 An X.509 crate at runtime | **rcgen at runtime, for the node only** (the recommended option). rcgen 0.14 is promoted from `[dev-dependencies]` to a runtime dependency of `gungnir-node` alone, its §2.9 row rewritten to say so and why: one crate already vetted, and remote signing through its `SigningKey` trait means the provider signs and the private half never leaves custody. The desktop keeps rcgen for tests. Not yet landed: the `SigningKey` implementation over `P256KeyProvider::sign`, the node issuing its identity through it, and the environment-path TLS becoming the development fallback. **Landed 2026-09-06**: `gungnir-remote/src/identity.rs` (moved there from `gungnir-node` the same day), rcgen a runtime dependency of `gungnir-remote` alone, §2.9 rewritten; reviewed and signed by the owner 2026-09-10 (GAP-060, `../../../ARCHITECTURE.md` §10 item 126). |
| D-30 The SecurityOfficer role | **Add `Role::SecurityOfficer` now** (the recommended option): a real variant with an empty layout (PN-20 and PN-09 only), no authority rule may name it for an operating action, `key.escrow_recover` is the one action it holds, every exhaustive match updated in one change, and DN-20's role table gains the row. Not yet landed. **Landed 2026-09-06**: the variant, its layout, its one action, DN-20's row; unsigned (GAP-084). |
| D-31 A YAML crate | **`yaml_serde`** (the recommended option): the YAML organisation's maintained fork of `serde_yaml`, same API, for `gungnir-scenario` only, to read the TT-01 to TT-10 scenario files and the catalogue tables. Enters §2.9 with the port. Not yet landed. **Landed 2026-09-06**: `yaml_serde` 0.10 in §2.9 and `gungnir-scenario`; `TrackLibrary` loads and cross-checks the four files (GAP-016). |
| D-33 Whether SD-16 (Cursor-on-Target) is in scope, and what it is pinned to | **In scope; three artifacts, two of them pinned.** SD-16 is release content and not a later increment: GAP-090 and GAP-091 target I3 with the stream sink in I4, so under D-01's rule the release carries them. That is an addition to the scope lock, taken deliberately and recorded, not an exception granted quietly. **The schema is pinned now**, ahead of the codec rather than behind it: the CoT event schema version 2.0 (13 June 2003, MITRE case #11-3895, approved for public release), with the attributes the mapping depends on transcribed in `../../design/external-standards.md` §5.2 -- including the finding that `ce` and `le` carry no stated confidence level, so the mapping must declare which multiple of sigma it writes. **The protobuf framing is deliberately not pinned**: it carries no version and no date, its only identifier is a commit of a GPLv3 repository, and nothing in the first increment needs it because negotiation begins in XML. The licence question a commit-pin would raise is deferred with the pin rather than answered by taking it. Edge (s), `gungnir-remote` to `gungnir-interop`, accepted the same day by the owner as engineering reviewer and recorded in `../../design/dependency-edges.md` §13. **Extended 2026-09-07 at the owner's direction: the type tree is pinned too**, which the 2026-09-06 answer had left open and `../../design/external-standards.md` §5.6 had named as the gap. The specification of record is MITRE's *The Developer's Guide to Cursor on Target*, August 2005, case #06-0249, approved for public release and read rather than summarised; the pin this system acts on is the guide's own `friend` predicate, the anchored case-sensitive expression `^a-f-`, and nothing else -- the other affiliations are recorded and act on nothing, so pinning their letters would be pinning from memory. Two findings came back to DN-25 with it: affiliation exists only in the atoms branch, so a positional read would invent one for a chat message, and case is significant, upper case being MIL-STD-2525B and lower case a CoT extension. **MIL-STD-2525 is deliberately not the pin**: the guide records that CoT's atoms tree is 2525B pruned, so pinning any edition of 2525 would name the ancestor rather than the artifact. The official predicate file is unobtainable (it ships with a non-public debugger) and the permissively licensed type-mapping file is named at revision 1.80 rather than copied, because one predicate is all this system asks of a type. **Extended 2026-09-08 at the owner's direction, adopting the recommendations of `../../design/tak-interoperability-research.md` §7 as written.** (e) **The framing is pinned now**, at `TAK-Product-Center/atak-civ` release tag 5.5.1.8, because the client's own contact manager starts a mesh client at protocol version 1 and drops to XML only for a contact that advertises nothing higher than 0: the I3 mesh feed and mesh sink meet protobuf on their first datagram, so 'nothing in the first increment needs it' was false and is withdrawn. The 2026-09-06 refusal is kept in `../../design/external-standards.md` §5.4 as history. (f) **Transcription**: the framing and the ten-message set are transcribed into `../../design/external-standards.md` §5.4.1 and §5.4.2, the codec's `prost` structs are hand-written against that table, no `.proto` file is copied and no code is generated from the reference tree -- the route §5.2 took for the XSD. Recorded as the owner's decision, not as a reading of the licence. (g) **ATAK-CIV on an Android phone plus WinTAK-CIV on the recording host** records the corpus, giving two senders, both wire forms and a client on each side of the emulator limitation; ATAK-CIV plus iTAK is the no-account alternative and tests iTAK's mesh wire form in passing. The recorder's `--tcp` and `--unicast` modes were built the same day; **the recording itself remains the one open step.** |
| D-32 Who verifies and pins AIS and ADS-B | **The agent verifies and pins, the owner reviews** (the recommended option): fetch M.1371-6 from the ITU and diff the eight message tables in scope against -5; copy gpsd's five AIS logs under `testdata/ais/` with a `SOURCE.md` on §1.5's terms; pin M.1371-6 and ICAO Doc 9871 2nd edition Amendment 2 in `external-standards.md` with what was and was not confirmed. The AIS codec follows the pins; ADS-B waits for a capture the owner records. The owner reviews the note before any codec is written. Not yet done. **Done 2026-09-06**: M.1371-6 pinned after the two editions were compared table by table (identical for the eight types; two backward-compatible additions read); the gpsd captures copied with `SOURCE.md` and the contributor headers stripped; the AIS decoder built and gated on gpsd's decodes. ADS-B: Doc 9871 2nd edition with Amendment 2 pinned as the specification of record; no permissively licensed capture exists, so the fixture is the owner's self-recorded one (GAP-010). |
| D-28 How MOP-37 gets its targets | **Round 1 on the built panels** (the recommended option): skip the wireframe round, run one participant per role against the rendered panels, and propose the MOP-37 targets from the measured values (median as target, worst case as floor, provisional under four sessions). The session package is written; the owner names participants and dates. Ten of sixteen tasks can run, six of those only once GAP-089 seeds the desktop with tracks and plans, and six tasks are round 2's because their panel is unbuilt or their write does not reach the desktop; those are reported as not run, never scored. **Re-examined 2026-09-08 against the code the round was going to be run on.** Three of D-28's own numbers had gone stale in the two days since: the allocator and the tracking pipeline the six-task split rested on both closed the same day D-28 was resolved (GAP-011, GAP-029), and GAP-050, GAP-057 and GAP-041/GAP-004 closed the day after -- so the round's queue-emptiness rationale for group B no longer holds, and three of the six 'round 2' tasks (US-04, US-08, US-09) and part of a fourth (US-15, via GAP-045's PN-16 rehearsal, landed 2026-09-08) are no longer blocked by an unbuilt panel or an unwired write. **The re-plan is held rather than executed**: proving this by tracing the seed found that the live allocator now re-proposes an unchanged assignment every tick, flooding the approval queue at the redraw cadence (GAP-097, filed the same day) -- a general defect, not a rehearsal artifact, since a node's own tick runs the same service and would flood identically under US-04/US-08/US-09. Round 1 does not run, on the ten tasks or the fourteen, until GAP-097 closes. Once it does, the plan is fourteen tasks (the original ten, plus US-04, US-08, US-09 and US-15, each carrying the heavier session setup a real node or a real rehearsal needs beyond the single `--rehearsal` flag), and US-07 and US-16 stay round 2's -- their blockers (an unwired control-status write; an undesigned gap-acceptance control) are unchanged. **Confirmed by the owner 2026-09-08**: the hold, the fourteen-task re-plan, and GAP-097's filing. **The hold lifted the same day**: GAP-097 closed, and with it the reason round 1 could not run on either scope. The session package is now prepared to the fourteen-task plan -- setups and a required dry run per group D task, both seed timing gaps fixed and pinned by tests that fail when they regress, US-15's card corrected to what PN-16 actually draws, and the report's stale seed hash replaced -- so what D-28 still owes is the fourteen sessions themselves and the target proposal they feed. That is the one part of this decision no agent can discharge: rule 3 above admits only measured values, and this round's participant is the owner. GAP-074 carries the detail, including a scenario-design question D-28 did not anticipate: PN-16 reads coverage from sensor placements alone, so round-1's two laydowns -- which differ only in where a battery stands -- cannot be told apart on the table US-15 compares them on. **The owner ruled on that question the same day and it is built**: `round-1.json` declares a third laydown `c`, the same two radars with S2 forward-sited 10 km up the declared approach, and US-15 now compares `current` with `c` on a measured difference of -5 250 m uncovered approach (7 000 m against 1 750 m) with `b` kept as the option that changes no coverage. It is scenario content only: the seed and its hash are untouched. |
| D-34 The repository's licence | **AGPL-3.0-or-later, with additional terms under section 7, a commercial licence beside it, and a CLA.** Chosen over the permissive options because of what the deployment profiles are: under Apache-2.0 or MIT a competitor could run a modified `gungnir-node` as a hosted service for a customer, never convey a binary, and owe nothing — and `../../../ARCHITECTURE.md` §8 makes exactly that shape the normal deployment. AGPL section 13 closes it. **Attribution is a term, not a request**: section 7(b) requires the `NOTICE` text be preserved in the Appropriate Legal Notices of a derivative's interactive interface, 7(c) forbids misrepresenting origin and requires modified versions be marked as different, and 7(e) declines trademark rights — the last of these restoring a protection Apache-2.0 §6 would have given for free and AGPL's base text does not. All three are in `../../../LICENSE-ADDITIONAL-TERMS.md`. **The commercial half is the reason the CLA exists**: `../../../CLA.md` takes a relicensing grant, without which one outside contribution freezes the dual licence permanently. **Two consequences that are not licence text.** Section 5(d) makes the interface obligation conditional on Gungnir's own interface carrying the notices first, so `gungnir-app` needs an About panel displaying them or every future fork is released from the requirement — **built 2026-09-07 as PN-21**, opened from the status strip so that every role reaches it and none docks it (`WorkspaceLayout::ALWAYS_AVAILABLE`), with a render test asserting all four of section 0's elements reach the screen and `appropriate_legal_notices.rs` asserting the panel still says what `NOTICE` says. And `deny.toml`'s allow-list is now a distributability control rather than hygiene: `OpenSSL` was removed the same day, being the only GPL-incompatible entry on it, though nothing in the resolved graph declared it. **Landed 2026-09-07**: `LICENSE` (AGPL-3.0 verbatim), `LICENSE-ADDITIONAL-TERMS.md`, `NOTICE`, `CLA.md`, `license = "AGPL-3.0-or-later"` inherited by all 50 crates, and the three-line SPDX header on all 365 source files. `testdata/asterix/` stays GPL-2.0 as an aggregate and gained the `COPYING` its redistribution requires. **Not reviewed by counsel**; plan 01 §10 owes that pass, together with the export-control question D-10 left open when it declined to fix repository visibility. |
| D-35 Theme tokens: flat constants, or a swappable Palette | **Keep the tokens as flat constants; file the night variant as its own gap rather than build it now.** Threading a `Palette` value through 239 call sites across three crates to make a token set swappable is its own tranche of work, not a corollary of the theme review that raised it. The night variant is GAP-095 -- filed as GAP-090, renumbered to GAP-094 the same day when GAP-090 was taken by an unrelated gap out of DN-25, and renumbered again 2026-09-07 on discovering GAP-094 was by then also carrying the advisories-gate gap, a second collision this row itself was silent about until the development-status review found it. |
| D-36 Selection halo colour: white, or a fourth map colour | **The halo stays white; the assignment line moves instead.** DS-04's rule holds -- white is reserved for selection and drawn by nothing else on the map -- and the line that sat too close to the friendly frame's blue is teal now, so the five colours sharing the map stay pairwise apart rather than the halo gaining a sixth. |
| D-37 Compared numerals: monospace, or the proportional face | **Monospace, through `theme::numeral`.** Every compared numeral -- time remaining, scores, positions, speeds, ages, evidence weights -- is set in egui's monospace face at body size so a column of them lines up; DS-05 gained a 16 pt size for a panel's title and nothing else, declining the six-size scale the same review proposed. |
| D-38 Viewport surface colour and coverage-ring alpha | **The blue-black viewport set and the coverage alpha, both already built.** `VIEWPORT_BACKGROUND` (8, 16, 24), `VIEWPORT_GRID_COLOR` (31, 56, 70), and `COVERAGE_MAX_ALPHA` at 80 stood unchanged by the review; recorded as its own decision because item 89 filed all four viewport and theme questions from one pass and this one needed no further code. |
| D-39 OS-keystore crate | **`keyring` 4.2.0, `v1` feature only.** Backward-compatible, auto-selecting per target: Windows Credential Manager, macOS Keychain, Linux Secret Service. Recorded in `agentic-coding-standards.md` §2.9 with the duplicate-linkage check, which found two real ones on the Linux target alone (a second RustCrypto generation and a second `zbus`, both `secret-service`'s and `zbus-secret-service-keyring-store`'s own pins, neither this workspace's to fix) and none on Windows or macOS. Built the same day as `PersistentKeyProvider::open_or_create_via_os_keystore` in `gungnir-security`, wired into `gungnir-app`'s `build_encryption` alongside the existing passphrase-sealed file; not wired into `gungnir-node`, which DN-22 §5 never assigned this row to. Human-owned code (`gungnir-security`, `gungnir-app`); written, gated, and signed by the owner 2026-09-08 (`../../../ARCHITECTURE.md` item 103, with items 104 and 111). |
| D-40 The inference runtime GAP-077 deferred | **`ort` (MIT OR Apache-2.0), un-deferring GAP-077 on the owner's own authority as the reviewer the deferral named.** Chosen over `tract` for full ONNX op coverage, since Plan 09's intended models are not yet known well enough to confirm they fit `tract`'s smaller pure-Rust subset. **`ort`'s default `download-binaries` feature is refused**: it fetches prebuilt ONNX Runtime from a third-party CDN, and those binaries may carry telemetry -- not acceptable for this system. Recorded in `agentic-coding-standards.md` §2.9 and built the same day: `2.0.0-rc.13`, `load-dynamic` rather than a from-source static link, so no C++ toolchain is needed to compile this workspace either way (confirmed with none installed at all). **`load-dynamic` turned out to be load-bearing rather than a convenience**: `ort` 2.0.0-rc.13 panics, and was confirmed by reproduction to panic twice over in a way that hard-aborts the process at exit, when no compatible ONNX Runtime is reachable -- true of every environment this landed in -- so `OnnxModel` sits behind `gungnir-ml`'s own `onnx-runtime` feature, off by default, until a real ONNX Runtime binary is known to be reachable. GAP-077's own entry carries the full account. |
| D-41 Real-world CRS support for DEM and point-cloud files | **`proj` (MIT OR Apache-2.0), binding `libproj` v9.6.x (X/MIT, OSGeo) via `proj-sys`.** Full projection support, not a WGS84-only first step: both geographic and projected (e.g. UTM) coordinate reference systems become usable for a DEM or a point-cloud file, closing the live usability gap where either loader refuses any file not already pre-converted to local-ENU. `proj-sys` finds a system `libproj` or builds it from source when none is found, the same two-path shape `vtkio`'s and `rcgen`'s own native dependencies already follow. Not yet recorded in `agentic-coding-standards.md` §2.9 or built; GAP-023 and GAP-098 carry the remaining engineering, including whichever validation and fixture rules `validate_terrain`/`validate_point_cloud` need once `"local-enu"` is no longer the only accepted frame. |
| D-42 Cloud KMS crate(s) for the ManagedService custody profile | **AWS and Azure, both via their official Rust SDKs: `aws-sdk-kms` (Apache-2.0) and `azure_security_keyvault_keys` (MIT, pre-1.0 at 0.9.0 -- noted rather than hidden, since a pre-1.0 crate's API may still move) with `azure_identity` for authentication.** `azure_security_keyvault` (the older, unofficial crate of a similar name) is explicitly not the pin: Microsoft has said it will not be updated further now that the official per-service crates exist. Neither GCP KMS nor HashiCorp Vault is in scope; a deployment needing either is a future decision, not a gap in this one. **Recorded in `agentic-coding-standards.md` §2.9 and built the same day**, after the design note this decision said was owed: DN-22 amendment 5 (§14) designs the `ManagedService` profile and `gungnir-security/src/managed_service.rs` implements it, both human-owned, reviewed and signed by the owner 2026-09-10 (`../../../ARCHITECTURE.md` §10 item 127). **Two corrections to this decision's own text, found when the crates were actually added.** `azure_security_keyvault_keys` is **1.0.1**, not pre-1.0 at 0.9.0: it reached 1.0.0 on 2026-05-13, so the 'a pre-1.0 API may still move' caveat above is retired rather than carried forward as a current fact, and `azure_identity` pins 1.0.0 rather than the 1.1.0-beta.1 that also exists. And **`aws-config` joins the two crates this decision named**, because `aws-sdk-kms` is the service client and carries no credential resolution at all -- the default chain that lets DN-22 §6's no-secret-in-a-baseline rule hold for a cloud client lives in `aws-config`. It is the crate AWS's own documentation pairs with every service SDK, so this is the decision's evident intent rather than a widening of it, and it is called out here rather than folded in silently. GAP-084 carries what remains: a real cloud round trip that needs credentials this workspace does not have; the owner's signature landed 2026-09-10. |
| D-43 What `solve_assignment` promises about a finite total | **`Assignment::total_cost` becomes `Option<f64>`; `None` means this problem's optimum is not a representable `f64`.** Chosen over the three answers the register framed, for reasons that were measured rather than argued. **Refusing the input was rejected**: no precondition can be both safe and tight, because which entries the optimum selects is not known until it has been solved, so any bound would refuse matrices whose optimum is perfectly representable -- and `assignment.rs`'s own module documentation already rejects that reasoning for rectangular padding, calling a large-enough pad value the kind of magic number that breaks quietly on a matrix with an unexpected scale. **Erroring on a non-finite total was rejected**: the pairing is provably correct in that case, and all three production callers consume only the pairing -- `GlobalNearestNeighbor::associate`, `gungnir-fusion-async`'s associate step, and `gungnir-metrics`'s CLEAR stage 2 each take `row_to_col` and discard the total -- so an error would have dropped a whole scan of tracking over a number none of them reads. **Promising nothing was rejected** as the weakest of the three on the table: it is true, and `-inf` is the correct IEEE-754 value of that sum, but it leaves a non-finite value reachable through a `pub f64` field on a function whose input guard exists precisely to stop non-finite values, guarded only by a doc comment -- the silent propagation `docs/agentic-workflow.md`'s low-trust tier names. **The `Option` is a fourth answer, raised when the owner walked the decision, and it says the one thing that is true**: there is an optimal assignment, and its cost is not a number. It refuses no solvable input, discards no correct pairing, and makes the unrepresentable case impossible to read by accident, because the type enforces it rather than the documentation. Its migration cost was near zero for the same reason erroring was wrong: nothing in production reads the field, so only the oracle diff test and the benchmark changed. **Built the same day** (GAP-103). Human-owned change, being a numerical stability guarantee: **written, gated and signed by the owner 2026-09-09**, the same day he took the decision and on his own owner authority. The signature covers the contract change, its tests, and the reconciled fuzz postcondition, and it was given with the one judgement call inside it stated rather than buried: floating-point addition is not associative, so `Some` versus `None` is exact about the solver's own accumulation and not about the ideal real-number sum, and making it order-free would need exact or scaled summation. That was judged not worth the added subtlety in a human-owned file whose one production cost matrix is bounded near 1.1e4 by construction, and `assignment.rs` documents the limit. |
| D-44 STANAG 4676 in this release, or out of it | **Descoped, and recorded as an amendment to D-01's scope lock rather than allowed to lapse quietly** -- the same way D-33 recorded an *addition* to it. STANAG 4676 leaves the first release: `Stanag4676Codec` keeps returning `NotImplemented` naming the reason, GAP-064 closes as ASTERIX-done-and-4676-deferred-by-decision, and GAP-063's conformance suite closes without the row its own action called the whole of what was left. The reason is the specification, not the effort: `external-standards.md` §2 records that neither STANAG 4676 nor AEDP-12 could be obtained in any verifiable form, and the owner's own note on this decision is that a NATO marking might forbid transcribing the tables into a public AGPL repository even if a copy arrived -- so retrieval could end in the same place after the cost. Writing the codec from secondary descriptions was refused by name: `external-standards.md` §2 calls that the written-from-memory outcome GAP-064 exists to prevent, and a conformance suite gated on a guessed wire format would be the kind of false claim this workspace's rules forbid. The route back in is unchanged and stays recorded: somebody with NSO access retrieves AEDP-12 Ed A v1, records its marking, and the gap is re-filed. |
| D-45 How predictions over a rehearsal become one first-engagement range | **The worst case: the minimum first-engagement range over the run**, labelled as the worst case and carrying the number of predictions behind it, rather than a mean, a median or a percentile. The argument is US-15's own table: it compares two laydowns on this one column, and a best-case or averaged figure flatters the weaker laydown on exactly the comparison the column exists to make. The count travels with it so a figure from three predictions is not read as a figure from three hundred. DN-02 §7 gains the rule as an amendment; GAP-020's predictor half is already built, so what follows is the aggregation and PN-16's column. |
| D-46 The dense-group pipeline's default filter | **PHD stays the default, and the choice becomes configurable** through the `TrackingConfig` vocabulary GAP-015's closing action already owed for the dense-group mode's settings. So a deployment that wants CPHD's cardinality estimate under clutter asks for it by name, and no shipped deployment's behaviour changes underneath it. `DenseGroupFilter::Phd`'s doc comment, corrected in item 128 to say the flip was an open question, now records the answer and the reason for it. |
| D-47 Whether a node's TLS identities persist, and whether the two roles share one | **One persisted identity, held by both roles.** A node is one machine identity, which is what D-02's mutual-TLS model already assumes and what a peer's trust store can pin across a restart; two identities would double the custody surface -- two keys, two rotations, two escrow records per node -- for a blast-radius argument that a single-process binary does not really earn. What it needs is the `gungnir-security` generalisation item 105 named: `PersistentKeyProvider::open_or_create_via_os_keystore`'s hardcoded desktop service name becomes a parameter, exactly as item 104 did for `wrapping_secret`, and `host_tls` and `spawn_tls_from_provider` then issue from the same persisted provider. Human-owned crate; it comes to the owner as a review. |
| D-48 What the wrapped-secret file records about the key that wrapped it | **Record the version.** `CloudKeyService::wrap` returns the key version alongside the wrapped bytes, `WRAPPED_SECRET_FILE` gains a small versioned header, and unwrap asks for that exact version instead of the latest. Chosen over documenting a rotation procedure because the failure the procedure would be guarding against is a node that cannot open its own keystore after a routine key rotation -- a lost journal record, arrived at by following ordinary operational advice -- and over re-wrapping at every start because a rotation while the node is down still strands the old blob, narrowing the window rather than closing it. It lands the way the rest of this profile did: tested against the `CloudKeyService` fake, with `#[ignore]`d tests naming the environment variables a real vault would need, and **no claim of a cloud round trip**. |
| D-49 Whether GAP-082's central differential-test harness is still wanted | **Retire the central harness; fix the document.** The distributed suite is what runs, what Gate 1 gates and what every oracle row in `../../verification-capability-table.md` cites, so `docs/agentic-workflow.md` §1 is re-pointed at where the differential tests actually live and GAP-082's central-harness intention is struck rather than built. Building it would duplicate what nine crates already do and add a second thing to keep in step with them -- and the drift this decision closes is the ordinary one this workspace's rules exist to catch: a document naming a vehicle that is not the vehicle. |
| D-50 Which of GAP-045's three undesigned controls are scoped now | **All three, scoped now** -- the owner's answer, against a recommendation to take only the first. The placement override is filed as GAP-105, the gap-acceptance control as GAP-106 and the rehearsal-gated submit as GAP-107. Two of the three have no design at all yet, so each gets a design note before code, and GAP-105 comes first regardless of that order because it is the one that makes a laydown comparison rest on real geometry: today a rehearsal cannot move the sensors a laydown declares, which is why round 1 had to declare a third laydown purely to produce a coverage difference US-15 could measure. Closing all three closes GAP-045's and GAP-087's tails and unblocks US-07 and US-16, which GAP-074 currently holds for round 2. |
| D-51 The CRS tail: whether `proj` becomes a default feature, and where the vertical datum lives | **`proj` stays opt-in, and the geoid model is filed as GAP-108.** Default-off stays because making it default puts libproj, cmake and SQLite3 in front of every contributor's build and every `cargo deny` run, for a conversion few deployments need -- and `deny.toml`'s `all-features = false` would then have to be revisited with new crates in the licence graph. The vertical datum gets a register entry of its own so that a converted height being the file's own vertical datum rather than a WGS-84 ellipsoidal height is a named gap in the summary table, rather than a sentence inside another entry's text, which is where GAP-102 left it. |
| D-52 Whether a cooperatively-identified UAS gets a platform class | **No platform class for a cooperatively-identified UAS; GAP-080's classifier owns the question.** The refusal `gungnir-app/src/uas.rs` already states is confirmed as the decision: Category 129's manufacturer and model strings are registration-authority allocations rather than a capability category, and the mission's air classes are drawn by threat type, so a lethality weight derived from one would be a guess wearing a number. A cooperative UAS therefore weighs 1.0 in `lethality_by_class` -- unknown is not harmless, which is the same rule the register already states for a track no source has classified. |
| D-53 How a reconciliation conflict is resolved | **D-03's rule resolves every reconciliation conflict it can rank, and a person resolves the rest.** A conflict is a decision on one side of an outage against a different decision, or an expiry, on the other. A real decision beats an expiry (DN-10 §9); otherwise the higher recorded role wins, and on equal rank the earlier decision. A side whose role was never recorded cannot be ranked honestly, so that conflict stays on PN-18 for a person permitted `plan.decide`, and the switch back waits for them; the journal says whether the rule or a person resolved each. For the rule to have something to rank, a decision records the role of the signed-in account that made it and none when nobody is signed in (DN-23 §5 rule 1), and the desktop's authority role follows the signed-in account, staying role-selected without a session (rule 5). The owner chose this over the recommendation, which was to record that a person resolves every conflict. |
| D-54 Whether a tasking concurrence may name nobody | **A requirement moves to Tasked only with a concurrence a signed-in operator gave; the criterion stands.** `TaskingCase::concur` refuses a concurrence that names no operator, and the desktop checks for one before issuing the command, so a refusal leaves no task behind. A deployment with no account store therefore cannot task through a requirement, and the refusal says so. A decline is outside the criterion and still records either concurrence. The MT-08 replay the row's method names is built over TT-08, whose sample events now script MT-08's first three steps (DN-11 amendment 3). |
| D-55 Where plan decisions live when desktops are linked to a node | **The node holds the queue for the desktops linked to it.** It authorizes each decision against the caller's role, takes one decision per item, opens the engagement, issues the handoff and journals all of it. While connected, the first valid decision on an item wins and a later one is refused naming the decision that stands (DN-10 §5's "whoever decides first ends it"). A desktop cut off from its node decides what its signed-in role may, with D-15's delegations lapsing after a configured interval, and forwards its decisions when the node answers, for D-53 to reconcile. The code both binaries need lives in one shared library rather than a copy on the node. Chosen over forwarding decisions without a shared queue, which would find a double decision only after both engagements had opened. |
| D-56 How decision, plan and queue-item identifiers become unique | **UUID v7, minted where each thing is created**, as `GlobalEntityId` already is. The field type changes, so by the interface's own rule `SCHEMA_VERSION` goes from 3 to 4 and the routes move to `/v3`. A journal written before the change still reads. Chosen over a machine-prefixed u64, whose uniqueness would rest on configuring every machine with a different prefix, and over provisional identifiers replaced on forwarding, which would rewrite journaled records. |
| D-57 Where the shared decision path lives, and its edges | **A new productization crate, `gungnir-approval`**, depending on command, policy, intercept-service, security, config, eventing and model (edge (w)); `gungnir-app` depends on it (x), and `gungnir-node` on it and on `gungnir-command` (y). Handoff delivery goes through a trait each binary implements with `gungnir-remote`'s endpoint client, so the library never reaches the transport. Each edge is drawn and recorded in the change that adds it to a manifest. |
| D-58 Whether reconciliation compares engagements as well as plans | **Reconciliation also compares engagements by track.** Two engagements of one track opened on the two sides of an outage are a both-may-have-acted incident for a person, published and alerted whatever their plans and whatever D-53's rule decided about any plan conflict, because choosing which record stands does not undo an effect in the world. |
| D-59 Whether a pre-delegated item escalates | **DN-10 §5: a pre-delegated item is actionable for the Operator from submission and still expires and escalates**, so a supervisor sees a delegated item nobody took. The code already does this; GAP-035's closing note was wrong and carries a correction. |
| D-60 How identifiers are written on the wire and in journals | **The standard hyphenated RFC 9562 string, still reading numbers.** The identifiers stay `u128` in memory, minted with `Uuid::now_v7()`; they serialize as the hyphenated form and deserialize from that form or a JSON integer, so every pre-change journal, fixture and test literal still reads, and a path parameter takes the hyphenated form or a decimal number. Implemented once, in `gungnir-model`. Chosen over `arbitrary_precision`, which changes how every number inside a field-tagged enum is buffered and breaks those enums, and over raw JSON bodies, which would change DN-18's exchange types and still hand a 128-bit number to readers outside Rust that round it. |
| D-61 How identifiers are shown | **A short tag on screen, the full identifier in the record.** Panels and alerts show the last eight hex digits as `…9f3a61c2`, the random end; PN-07 and the handoff panel's detail show the full identifier with a copy control; audit entries, journals, reports and log fields carry the full form. A rehearsal seed's plan (`P-1183`) keeps the number the seed gives and is shown as given. Chosen over full UUIDs everywhere, which crowd a queue row and are told apart only at the end, and over per-desktop labels, which would give one decision different names on different desktops. |
| D-62 Whether gate 6 ever enforces its threshold on this runner | **It never enforces on this machine.** Gate 6 stays advisory on `gungnir-rtx-5060ti`: it runs, compares, and prints every ratio and every threshold breach for a person to read, and it does not fail a pull request. A regression is found by reading those ratios, by the budget assertions in `gungnir-app/tests/frame_budgets.rs`, which are pass criteria and unaffected, and by the absolute figures in `../../performance-budgets.md`. Chosen over widening the threshold, which would make a gate criterion say less than it does now, and over a multi-run baseline or a statistical comparison, which is engineering spent on a host whose noise is the owner using it. The `pull_request` trigger does not come back. Enforcement would need a different machine, which is not in scope for this release. See `../../record/2026-09-17/gate-6-stays-advisory-on-this-runner.md`. |
| D-63 Whether the self-hosted runner is installed as a service | **It stays interactive.** No service install, and no separate account for it. The runner is started by hand when it is needed. Nothing shipped depends on it: no release artifact is built there (`release.yml` builds on hosted runners), and the two workflows that use it are gate 6, now advisory by D-62, and `gpu-fusion.yml`, which is manual dispatch only. So an offline runner delays re-evidencing the GPU path row and a benchmark comparison, and blocks nothing else. See `../../record/2026-09-17/gate-6-stays-advisory-on-this-runner.md`. |
| D-64 How a rehearsal observes a laydown's sensors | **Generated at rehearsal time, in the production binary.** Chosen over committing a fixture per laydown, which keeps the dependency rule intact but only ever covers the laydowns somebody generated fixtures for. The cost is accepted deliberately: the operational binary gains an observation model it does not need for live operation, so the design carries the containment that keeps a synthetic detection out of every live path, and the `gungnir-ingest` gateway's refusal is human-owned. Decided 2026-09-16 and recorded here 2026-09-17: the design it belongs to sat unmerged on a branch, and the id it first claimed (D-53) was taken by the GAP-067 walk in the meantime. No code exists; `../../signatures.md` is where the design's signature status is stated. |
| D-65 Whether the decision path is human-owned wherever it runs | **It follows the code.** `gungnir-approval` and the node loop that runs it (`gungnir-node/src/approval.rs`) are human-owned, added to the list in `../../../agentic-workflow.md`. The rule is about a boundary -- nothing executes an intercept without a recorded human decision -- rather than about the three crates that happened to hold it, so a change that moves the boundary's code moves the ownership with it. Raised rather than folded into the change that moved it, on the precedent the same list records for `gungnir-remote`'s identity path (GAP-060, answered 2026-09-06), and answered the same way. |
| D-66 The order a reconnecting desktop settles, forwards and switches back | **Settle first.** The merge comes first -- the plan conflicts and, in the same pass, the engagements by track, with `BothActed` published before the rule settles anything (D-58) -- then the whole outage is forwarded as one batch carrying each settlement, which the node applies whole or not at all, then a person switches back (D-15). Recorded as DN-31 amendment 2 (§13), with `ForwardedDecision`'s `settled` field, `LinkEvent::DelegationsLapsed`, and the build's finding that plan conflicts cannot arise across an outage as built, so D-58's comparison by track is what catches two actions on one track. See `../../record/2026-09-17/an-outage-reaches-the-node-once.md`. |
| D-67 How a desktop is named, and what it forwards when its key is ephemeral | **By its own key, and an ephemeral desktop still forwards and says so.** A desktop is `desktop-` and sixteen hex digits of a SHA-256 over the public half of the key its certificate carries, so the name is a function of what the handshake proved rather than a string a self-signed certificate asserts, and the node can check a forwarded batch's origin against the party it verified with no configuration. A desktop whose key is ephemeral is named `desktop-ephemeral-` and the same fingerprint: it forwards as before, because refusing would keep every offline decision off the node's record (DN-31 §9 row 8), and the name says what the origin is worth. A stated, readable label was not taken: two desktops could claim one name and nothing would detect it. See `../../record/2026-09-22/a-desktop-is-named-by-its-own-key.md`. |
| D-68 How the node's own exchange set and a desktop's published set share one register | **One set per producer, merged on read** (DN-18 §11). The register is keyed by item and then by the name the connection was verified under -- since GAP-141 a digest of the desktop's own key (D-67) -- with the node's own set a variant of its own so no desktop name can collide with it. A publish replaces that producer's set alone; a read concatenates every producer's, the node's first, and answers `NotHeld` only when none holds any. The node as sole writer would have needed a collision rule for two desktops claiming one product, and that rule would have lived in the transport; append per handoff cannot express a withdrawal, which DN-18 §5's "partial delivery is reported" turns on. Bounded at 64 producers an item, refused rather than evicted. The owner answered as proposed on 2026-09-23, the day after the build. See `../../record/2026-09-23/a-partner-hears-what-the-node-decided.md` and `../../record/2026-09-23/the-register-s-contract-is-decided.md`. |
| D-69 What makes a producer's exchange set stale, and what a partner is told about it | **Nothing expires, the answer carries its age, and a desktop refreshes when its link comes back** (DN-18 §12). A handoff is a decision that was taken, so expiry would delete a true thing to hide an unknown one, and a partner would watch a list shrink for a reason no field explains. `as_of` on both `ExchangeResponse` variants is the node time the least recently refreshed producer wrote, which is what a partner can be told without learning how many consoles this deployment runs. A desktop publishes its whole handoff set the tick its link returns -- the edge, not the state -- and one holding none publishes none. The bound is unchanged: a new producer beyond sixty-four is refused rather than evicting a set a partner is served from. See `../../record/2026-09-23/a-register-that-says-how-old-it-is.md`. |
| D-70 Whose clock a node's deadline is drawn against, and when a console is told they differ | **The node stamps its clock on the snapshot as it answers, the desktop measures the offset once per connection and draws the node's deadlines against the node's time, and PN-01 says the difference above one second** (DN-31 §14). The pair of readings is kept rather than the difference, so a countdown advances on the desktop's own clock between snapshots while staying on the node's scale. One second is the threshold because a countdown is drawn to the second: below it nothing a person reads can change, above it the console and the node disagree by a digit on the screen. Nothing sets or steers a clock. See `../../record/2026-09-23/a-deadline-in-the-node-s-own-time.md`. |
| D-71 What a desktop that has never reached its node does | **Silent, measured from the link's own start** (DN-31 §15). The link records when its task began asking, and silence runs from the last thing heard or from that start where nothing has ever been heard, against the same heartbeat timeout as any other silence. The desktop falls back and the sentence says which it is: "has not answered since this desktop signed in" rather than "silent for 12 s", because an operator reads the two differently. See `../../record/2026-09-23/an-outage-outlives-its-process.md`. |
| D-75 What a desktop's link does with a publish the node refuses, and what the console is told | **Taken under the owner's delegation of 2026-09-25.** Read each answer for what it says about the caller (DN-18 §13): `2xx` delivered; no answer, `408`, `425`, `429` or a `5xx` (the full register's `507` included) is "not now", retried after an interval doubling from one forward tick to thirty seconds and reset on every new connection; `401` or `403` is "not you", and the link keeps every set and offers nothing more until it signs in again, which every connection does and which a sign-in on the console brings about; any other `4xx` drops that one set, counted. PN-09 carries one line for a linked console, giving the node's reason, what is held and the roles the authority matrix lets publish, read from the matrix itself; nothing goes to the alert list. The reconnection edge republishes launch warnings with handoffs. A timed retry of a `403` was rejected because it asks the same session the same question; stopping the Operator's console from queuing at all was rejected because the node, not the desktop's copy of the matrix, answers for the session, and the held set is what a Supervisor signing in at that console should send. See `../../record/2026-09-25/a-console-that-may-not-publish-says-so.md`. |
| D-76 How the desktop's exchange outbox is bounded | **Taken under the owner's delegation of 2026-09-25.** One set per item, the newest (DN-18 §13). Every set is its producer's whole current set, so a newer one makes any older one waiting for the same item obsolete: the outbox replaces it in place and counts the replacement, and a set already on its way is removed by its generation so a newer one survives. A capacity with the oldest dropped was rejected because it would keep obsolete sets while dropping by age across items, and post every obsolete set on reconnection; `StoreAndForwardQueue` was rejected for the same rule over a different type and for a `gungnir-remote` to `gungnir-resilience` edge `../../../ARCHITECTURE.md` §7.1 does not show, and GAP-121 is left open. See `../../record/2026-09-25/a-console-that-may-not-publish-says-so.md`. |
| D-79 Which layout engine the If-Sr diagrams are rendered by | **Taken under the owner's delegation of 2026-09-25.** Every If-Sr diagram, the overview included, is laid out by Graphviz dot, and the render scripts pin the Docker image, `plantuml/plantuml:1.2026.8`, whose SVGs record the version that drew them. Measured with that image on all nine: dot's left-to-right layouts cover 1.05x to 1.66x smetana's area, and the 30-40x the pin's comment recorded was the top-to-bottom default, which left-to-right cures under either engine. A per-diagram engine was rejected because it keeps two engines in one family, which is the defect; a fixed version was rejected because none exists (1.2026.8 is the latest image), and smetana would stay one graph change away from the next crash while the check needs every element positioned. The Rs-Cn family renders cleanly under smetana and keeps it, one engine per family. See `../../record/2026-09-25/housekeeping-after-dn-31.md`. |
| D-80 Whether the unreachable plan-keyed decision type is removed or documented | **Taken under the owner's delegation of 2026-09-25.** Removed, both. A decision is taken on a queue item through `DecisionRequest`, whose caller is whoever the session token says; a second, plan-keyed description that names an operator is a contract nothing honours and every reader has to be told not to believe. Documenting it was rejected because no implementation is planned that would take a decision on a plan rather than on the item that carries its deadline and its roles (DN-31 §7). `ApiHandler` keeps its two methods, both of which describe routes the node serves, and now says that nothing implements it. The UAF's IE-23, the operator's decision carried over the API, names `DecisionRequest` as the type that carries it. Human-owned: a `gungnir-api` write path; see `docs/signatures.md`. See `../../record/2026-09-25/housekeeping-after-dn-31.md`. |
| D-77 How the journal holds an envelope carrying a NaN or an infinity | **Taken under the owner's delegation of 2026-09-25.** The journal carries it, losslessly: an envelope whose floats are all finite is written byte for byte as before, and one that carries a non-finite float is written as a marked line (`~`) in which each such float is a string token holding its bits and a genuine string that could be mistaken for one is escaped. `append` re-reads a marked line before writing it and refuses with `StoreError::Unfaithful` a line that would not come back as written. A refusal was rejected because a real producer can emit one (a deadline typed as "inf" into PN-10, now also refused where it is typed), and a refused envelope is a lost event: the record of the fault is exactly what an investigation of it needs. A per-field serde attribute was rejected because several hundred float fields reach the journal, some inside internally tagged enums serde buffers first, and one forgotten field would reopen the defect; the adapter wraps `serde_json` once and covers every field by construction. See `../../record/2026-09-25/every-float-kept-old-sessions-purged-on-purpose.md`. |
| D-78 When the journal is purged, what a purge may never take, and who chooses the age | **Taken under the owner's delegation of 2026-09-25.** Retention is declared in the baseline (`ConfigBaseline::retention`) or nothing is purged, and both binaries say which at start: the period is the customer's record-keeping obligation (data architecture §4), and a default nobody chose must not begin deleting mission records on an upgrade. Age is days since the session's journal was last written, from the file's own time, so a session sealed under a key a later process cannot open is still reached. Both binaries purge after the live session exists and hourly; never while opening the journal. Never purged: the session open for appending, a session under a hold file (placed and released by the after-action review, or by an administrator), and what the caller protects -- on the desktop, every session an open requirement's events are in, the sessions holding the highest requirement identifier and launch-warning serial, and an unfinished outage's session. A purge renames the journal out of the listing, removes the mission record, then deletes the file, so an interrupted purge leaves every session whole or gone and the next finishes it. Each removal is a `RetentionEvent` in the live session, a log line, a count, and on the desktop an alert. The audit-log age governs nothing until a durable audit log exists (GAP-152). See `../../record/2026-09-25/every-float-kept-old-sessions-purged-on-purpose.md`. |
| D-83 How the risk score reads time to impact | **Taken under the owner's delegation of 2026-09-25.** **Time to impact stays range to the boundary over closing speed, and a closing track scores on it alone** (DN-01 §10). Urgency is `τ / (τ + T)` with `τ` the baseline's `assessment.urgency_half_time_s` (60 s by default); a confidently closing track's kinematic factor is `1/2 + u/2`, one not closing keeps `p/2` on proximity, and the track's own closing-speed sigma blends the two, so a closing speed within two sigma of zero earns little. Priority and lethality multiply it as before. The time to the closest point of approach was rejected as the time to impact: it ignores the miss distance, so a track passing 20 km off would be "10 s from impact" and trigger impact warnings DN-03 keeps for arrivals, and it halves the score the instant a track passes. A range-and-time product was rejected because a near-slow track then outscores a far-fast one that arrives sooner, which is the defect. See `../../record/2026-09-25/time-to-impact-and-sensor-sectors.md`. |
| D-84 What a sensor's azimuth sector is and where it is stated | **Taken under the owner's delegation of 2026-09-25.** **A sector, as a boresight and a width in (0, 2π], against true north at the sensor, on the sensor's declaration and optionally on a laydown placement; absent is the full circle** (DN-12 §9). Removing bearing from the criterion was rejected: real sensors have sectors, and a panel radar counted as covering all round is coverage that is not there, which DN-12 exists to prevent. True north at the sensor because that is what a survey states; the local frame's north departs from it by the meridian convergence, 0.35 degree 39 km east at 45 degrees north, so the frame turns each sector as it places it. Two edges were rejected for a boresight and width because the width is what validation bounds and a sector across north then needs no special case. A placement may re-aim its sensor, and one that does not inherits the declaration's sector. See `../../record/2026-09-25/time-to-impact-and-sensor-sectors.md`. |
| D-85 What "identical projections" means for a plan kept from an earlier solve | **Taken under the owner's delegation of 2026-09-25.** The picture, the health strip, PN-09's bearing line, the withheld resources, the scenario's alerts and a selected track's what-if are compared exactly; a linked desktop's plan is compared exactly with the node's; the two backends' plans are compared by their pairing, and their planners by one solve of the final picture at one time, equal in all but the minted identifier. Comparing plan vintages exactly was rejected because it would pass or fail on the scheduler; comparing them within a tolerance was rejected because it would hide a real planner difference behind one -- the common solve is what caught the node's missing frame. The row and its criterion are the owner's walk. See `../../record/2026-09-25/one-scenario-through-both-backends.md`. |
| D-86 How a node's picture stays live on a linked desktop between snapshots | **Taken under the owner's delegation of 2026-09-25.** The node diffs its picture each tick against what it last announced and publishes `TrackInitiated`, `TrackUpdated` for any changed field, and `TrackDeleted`, deletions first so a reader appending keeps the host's order (`TrackLifecycle`, `picture::Announcer`); the link keeps the node's health from the snapshot and every `HealthEvent::Changed`, and a remote service is healthy only when the link is up and the node says its service is. Re-reading the snapshot was rejected: it polls the whole picture per desktop, leaves the journal, the partner stream, the reports, the replay and the entity fold without the events all five were written against, and delays every change by the interval. See `../../record/2026-09-25/one-scenario-through-both-backends.md`. |
| D-72 What truth a rehearsal re-observes, when scans fall between truth records | **Taken under the owner's delegation of 2026-09-25: the recording's own tick, by the recording's own rule.** A scan at a given time sees the truth record of the first tick at or after it, which is exactly what every recorded detection saw, so no position is ever computed between two records; a truth record off the tick grid is refused. The finer tick was rejected because it would not have delivered its own aim -- every sensor's first scan is at a random phase, so scans would still fall between 0.5 s samples -- and because it would have rewritten all ten committed sets, which gated rows elsewhere measure, for no gain in what §5.1 protects. Interpolation stays refused, as §5.1 said. DN-32 §12; see `../../record/2026-09-25/gap-105-re-observation-for-a-laydown-built.md`. |
| D-73 Whether a recording's sensor losses and jamming apply to a deployment's sensors | **Taken under the owner's delegation of 2026-09-25: counted, not applied, and the sea state applied to every sensor.** A window naming the recording's sensor is a fact about that sensor, and applying it by identifier is the accidental match DN-32 §5.4 refuses for detection models. Transfer by type was rejected because jamming depends on where the jammer stands, which no recording states, so a transferred window would invent an effect. The rehearsal record counts what it did not apply and PN-16 says so; when the placed sensors are the recording's own, as in the statistical check, every event applies by identifier as the generator applied it. See `../../record/2026-09-25/gap-105-re-observation-for-a-laydown-built.md`. |
| D-74 How a rehearsal's random draws are seeded | **Taken under the owner's delegation of 2026-09-25: common random numbers, keyed by the recording's seed and by sensor and target, never by the laydown.** One stream per sensor for its scan phase, one per sensor for its false alarms, one per sensor and target for detection. The same laydown still gives the same answer twice, which is what §7 asked, and two laydowns differ only where their geometry does: with round 1's baseline, `current` and `c` give S1 the same 634 detections and S2 1 241 and 1 260, and the comparison names S2 alone. One interleaved stream, as the generator uses, was rejected for the same reason as seeding by laydown: moving one sensor would reshuffle every other's detections. See `../../record/2026-09-25/gap-105-re-observation-for-a-laydown-built.md`. |
| D-89 When a linked desktop renews its session with the node | **Taken under the owner's delegation of 2026-09-25.** Both, on the link: it renews at three quarters of the lifetime the node issued -- the expiry against the node's own time on the snapshot -- and at once whenever a request is refused `401`, keeping what it offered and sending it again under the new token. A `401` is never delivered as an answer to a decision, a task or an exchange set. A refused renewal ends the connection, so the link is seen down rather than looking up while every write is refused. The desktop's own session still bounds the link: an expired desktop session drops it. Renewing only on refusal was rejected because every lapse would cost a refused request and a round trip on the path an operator is waiting on; tokens that do not expire on a live stream were rejected because DN-23 §5 makes a node-issued session always expire, and that is a `gungnir-api` authentication change. See `../../record/2026-09-26/a-live-link-keeps-its-session.md`. |

## Consequences for the register

- D-05, D-11, and D-12 created engineering items: GAP-068 (roles in code), GAP-069
  (`uuid` v7), GAP-070 (display vocabulary).
- D-07 moved GAP-036 (fires) from I4 to I3 and put fires deconfliction in the policy
  model; the fires handoff message is defined in I3 and delivered over the transport
  with GAP-040 in I4.
- D-10 changed GAP-061 from running the GitHub workflows to porting them to GitLab CI
  on self-hosted runners; effort S to M. **Amended 2026-09-07 to GitHub at
  `github.com/WayneRoessling/gungnir`**: the port is gone and the eight workflows stay
  where they are, but the effort stays M, because what gates the gap is not the port. The
  workspace is not a git repository, so the gap starts with an initial commit, and
  `gpu-fusion.yml` still needs a self-hosted runner labelled `gpu`. **On 2026-09-07 that
  workflow was made dormant** (manual dispatch only): with no runner registered, every
  pull request touching the two crates it watched queued a job that failed a day later,
  and the job itself ran zero tests, because the GPU path and its tests are GAP-024's
  and are not built. It now fails a run that executed nothing, so the runner alone
  cannot pass it.
- D-13 fixed the closing action of GAP-021 without a new dependency edge.
- D-01 confirms that no gap is out of scope; every I4 item is release content.
- Plan 07 (test-track suite, 2026-09-04) delivered the catalogue, class profiles,
  sensor models, scenario library, reference generator, and ten validated sample sets;
  GAP-046 moves to In progress and GAP-076 covers the remaining integration.
- Plan 06 (UX designs, 2026-09-04) added GAP-071 to GAP-074 (replay, reports, and
  configuration panels; the status strip; the evidence card, commander summary, and
  theme additions; the usability test rounds) and raised D-17 (docking and
  multi-window), resolved the same day.
- Plan 09 (ML model integration, 2026-09-04) added GAP-077 to GAP-080: the
  `gungnir-ml` crate and the inference-runtime sign-off, model manifests as
  `gungnir-modelops` baselines, the dataset pipeline, and the first two models.
- Plan 08 (AI agent integration, 2026-09-04) answered D-14 and kept GAP-044 as the
  single assistant item; the crate, the providers, the tools, the panel, and the
  evaluation harness are all inside it.
- Plan 11 (design gap closure, 2026-09-05) reviewed the design gaps and applied four
  corrections: GAP-052 is retyped Mission because nothing is designed; the CAP-6.1
  coverage row is refreshed now that D-02 is decided; GAP-084 is filed for key custody
  and GAP-060 narrowed to ciphers; GAP-026 is cross-listed on CAP-5.6.
- Plan 11 also closed the design gaps themselves: twenty-two design notes in
 `../../design/` name a component, its types, its edges, its behaviour, and its
 verification row for each. Twenty-three gaps are retyped Mission to Technical
 because they are now designed and awaiting implementation, and twenty-two
 capabilities reach full design coverage. The notes are first drafts awaiting
 the owner and the reviewers.
- D-18 (2026-09-05) signed off the API transport stack, the last decision
  standing between the decided contract and the code, so GAP-041's closing action is now
  implementation alone. The owner moved GAP-041 from I4 to I2 on 2026-09-05: it carries
  severity 5 across six mission threads, which is higher than most of I3, and until it
  exists only the disconnected profile can be exercised and MT-10 cannot run end to end.
  GAP-050, GAP-063, and GAP-065 inherit both changes through their dependency on it.
- D-19 (2026-09-05) signed off `egui_tiles` 0.10 as the docking crate, closing the
  last open §2.9 question. It enters a member manifest under GAP-075.
- Plan 10 (TOGAF ADM, 2026-09-04) ran the first compliance assessment against the
  code and added GAP-081 to GAP-083: automating the five mechanical contract checks,
  proving no `todo!()` is reachable, and making requirement identifiers traceable.
- D-34's licence gate was **run for the first time on 2026-09-07**, in the licensing
  review that followed the decision, and failed twice, both on the `deny.toml` comment's
  claim that `OpenSSL` had been the only entry that mattered. `epaint_default_fonts`,
  which `gungnir-app` embeds, declares the SIL Open Font License and the Ubuntu Font
  Licence for its font data, neither on the allow-list; and `webpki-root-certs`
  (CDLA-Permissive-2.0) sat in the wasm32 slice of the graph that cargo-deny evaluates
  by default and no binary builds. The first is admitted by a `[[licenses.exceptions]]`
  scoped to that one crate, with the reasoning in `deny.toml`; the second is answered
  by restricting `[graph] targets` to the two release targets. The same review found
  that nothing shipped the notices the permissive licenses require to accompany a
  binary: `about.toml` and `cargo about` now generate `THIRD-PARTY-NOTICES.md` in the
  release, the node image carries the licence files, `ci.yml` runs the licence check on
  every pull request, `gungnir-fuzz` (outside the workspace, so unable to inherit) now
  states the licence, and `testdata/oracles`, `tracks`, `assets` and `scientific` gained
  the `SOURCE.md` that `NOTICE` already said they had. Two further failures were the
  gate's own shape rather than a third-party licence: cargo-deny holds the workspace's
  crates to the same allow-list, and `AGPL-3.0-or-later` is deliberately not on it (a
  global entry would admit an outside AGPL crate that CLA.md section 4 cannot accept),
  so the fifty members are admitted by name in `[[licenses.exceptions]]`; and the
  wildcard ban reads a versionless `path` dependency as a wildcard, so every
  intra-workspace edge now carries `version = "0.1.0"`, which crates.io requires anyway.
  With those, `cargo deny check licenses bans sources` passes. Every `Cargo.toml` now
  carries the same three-line header as the source files (fifty-two manifests, the root
  and `gungnir-fuzz` included). Consistency is one reason; the other is that cargo-about
  looks for the licence expression *in the manifest text*, and a manifest that inherits
  it through `license.workspace = true` has none, which sends cargo-about 0.9.2 down a
  synthesised-manifest path with an offset bug that panics on any manifest containing a
  non-ASCII character -- forty-two of them do, from the `§` citations. The header's SPDX
  line is the text it finds. A second cargo-about defect, in its `LicenseRef` handling,
  is recorded in `about.toml` with the template's workaround. **`cargo deny check
  advisories`, which `release.yml` also runs, does not**: `vtkio` 0.6.3 pins `quick-xml`
  0.22 and `lz4_flex` 0.7, both with open RUSTSEC vulnerabilities; the Linux
  accessibility path under `eframe` (`accesskit_unix`, `zbus_xml`) carries `quick-xml`
  0.30 with the same two; and six crates are flagged unmaintained (`ansi_term`,
  `cgmath`, `instant`, `paste`, `rustls-pemfile`, `ttf-parser`). None is a licensing
  matter; it is recorded here because the first tag will otherwise discover it, and it
  is a dependency decision under §2.9, not something the licensing review could take.
  **Acted on the same day for the unmaintained six**, after weighing forking against
  replacing: `rustls-pemfile`, the only one of the six that was a direct dependency and
  the only one parsing input on the mutual TLS path, was replaced by the PEM reader in
  `rustls::pki_types::pem` (rustls's own re-exported type crate, so the graph lost a
  crate, gained none, and no manifest names anything new); `ansi_term` (dev-only) and `paste` (a proc-macro, compile-time
  only) are ignored in `deny.toml` with dated reasons; `instant` left the same day
  with three-d's default `window` feature, which the viewport never used (it draws into
  eframe's glow context; the feature carried a second winit, 0.28, and glutin beneath it);
  `cgmath` is three-d's own math type, still a dependency of three-d 0.19, and stays
  until three-d drops it or is replaced (0.19 is ruled out by its move to glow 0.17 and
  egui 0.34 against eframe 0.29's glow 0.14); `ttf-parser` waits on egui's `ab_glyph`
  moving to its successor. **The owner decided on 2026-09-07 to ignore both** rather
  than hold releases on an upstream with no date: `cgmath` is pure math on data this
  program computes, and `ttf-parser` parses only the four fonts embedded at build time
  (nothing here builds a `FontDefinitions` or reads a font from disk), so neither sees
  untrusted input, and neither has an upstream path. Both entries in `deny.toml` carry
  that reason, and the second carries a condition: loading a user-supplied font would
  need it removed first. Forking was rejected: the graph refuses git
  dependencies, so a fork means vendoring, and owning a font parser with no upstream is
  a larger accreditation question than the advisory it would close.
  **The three vulnerabilities followed the same day.** `vtkio` 0.6.3's XML reader
  (quick-xml 0.22: RUSTSEC-2026-0194 and -0195) and its appended-data codecs (lz4_flex
  0.7: RUSTSEC-2026-0041) have no patched upstream, since even the 0.7.0 candidates pin
  quick-xml 0.36 against a fix in 0.41, and a VTK file is operator-supplied input on the
  node. Both features are compiled out: only legacy `.vtk` loads, `gungnir-data` refuses
  an XML extension naming the reason, a test holds that wording, and the workspace
  `Cargo.toml` says what restores them. GAP-023's "legacy and XML" therefore narrowed to
  legacy on 2026-09-07; the XML half is owed to a vtkio release, not to this project.
  The remaining quick-xml (0.30, under `accesskit_unix`, the Linux AT-SPI bridge) exists
  only in `gungnir-app` compiled for Linux, which no release builds; its two advisory ids
  are ignored in `deny.toml` with that reason and with the caveat that an id-level
  ignore would also mask a new pre-0.41 quick-xml, which the ignore's own match report
  is the check for. With those two ignores, `cargo deny check` passes all four checks
  for the first time.
- D-34's rows in both tables above were **lost on main on 2026-09-07** by commit
  `0ac4d4b` (GAP-057), whose copy of this file predated the decision; the same commit
  dropped D-35 to D-38, which the renumbering commit `34796a6` had just added. The
  rebase of the licensing review restored D-34, since it is that review's subject.
  **Restored 2026-09-07** by commit `a6bc70f`, which came after this paragraph was
  first written and left the paragraph itself saying they were still missing;
  corrected 2026-09-08 on finding the two disagreed. They belong to the theme tranche
  that owns them; `../../ux/design-system.md` also cites them.
- D-34's one engineering item is **closed 2026-09-07**: `gungnir-app` displays the
  Appropriate Legal Notices as PN-21, so the section 7(b) interface requirement now
  reaches a derivative under AGPL section 5(d). PN-21 is the twenty-first panel and is
  in `../../ux/information-architecture.md` §3 and `ux-to-code-map.md` §1; it is in no
  role's layout by design, and two tests hold that open.
- D-40, D-41 and D-42 (2026-09-08) each un-blocked an owner-only judgement call the
  register had been carrying: the ML inference runtime GAP-077 deferred (`ort`, native
  dependency accepted, `download-binaries` refused for its third-party-CDN telemetry
  risk), real-world CRS support for DEM and point-cloud files GAP-023 and GAP-098 both
  needed (`proj`, full projection support), and the cloud KMS crates GAP-084's
  `ManagedService` profile needed (`aws-sdk-kms` and `azure_security_keyvault_keys`).
  D-40 and D-41 are decisions only: neither is yet recorded in
  `agentic-coding-standards.md` §2.9 and neither is built. **D-42 is recorded and built**,
  2026-09-08: §2.9 carries its crate table (with the correction that
  `azure_security_keyvault_keys` is 1.0.1 and not the pre-1.0 version the decision recorded,
  and with `aws-config` added for the credential chain the service crate does not carry), and
  DN-22 amendment 5 designs the profile the crates serve. STANAG 4676 (GAP-063, GAP-064) was walked the same session
  and the owner declined to purchase the paywalled specification, leaving that gap's
  status unchanged.
